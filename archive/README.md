# Archive

Historical one-offs moved 2026-08. Not runtime. Do not execute against production DB.

Everything else pruned in the 2026-08 cleanup was hard-deleted; recover via git
history (`git log --diff-filter=D --oneline -- <path>`).
