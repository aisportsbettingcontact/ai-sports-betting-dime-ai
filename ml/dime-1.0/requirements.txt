# Dime 1.0 training pipeline — pinned for reproducible runs on a RunPod Pod
# (PyTorch template already provides CUDA-matched torch; keep it if present).
torch>=2.3.0
transformers==4.44.2
datasets==2.21.0
peft==0.12.0
trl==0.9.6
bitsandbytes==0.43.3
accelerate==0.33.0
autoawq==0.2.6
huggingface_hub>=0.24.0
requests>=2.32.0
