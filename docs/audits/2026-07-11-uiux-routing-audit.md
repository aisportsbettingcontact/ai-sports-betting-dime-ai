# UI/UX + Routing Audit — 2026-07-11 Navigation Reconstruction

10-subagent audit of the canonical-routing reconstruction (branch
`claude/ai-projections-routing-fix-cqq7zm`): 5 adversarial routing verifiers +
5 UI/UX lenses (mobile / tablet / desktop / motion / brand+a11y) over
DimeModelFeed, BettingSplits, the mobile owner tabs, and the chat gate.
P0/P1 items on touched surfaces were fixed in-branch; each finding below
carries its disposition.

## Verifier verdicts (routing contract)

### feed-routes — PASS

- ✅ Routes registered behind RequireAuth: client/src/App.tsx:179 (/feed/model/:sport/:date) and client/src/App.tsx:180 (/feed/model/:sport), both wrapping DimeModelFeed in <RequireAuth>; dated split-form route precedes bare-sport route so /feed/model/mlb/07-11-2026 binds date correctly
- ✅ Route-order safety: the /feed legacy redirect (client/src/App.tsx:174) compiles via regexparam to ^/feed/?$ with the 'i' flag (verified against node_modules/regexparam/dist/index.mjs) — it cannot swallow /feed/model/*, matches trailing-slash and mixed-case forms, and wouter route params arrive case-preserved
- ✅ parseFeedModelPath (client/src/pages/DimeModelFeed.tsx:252-271): lowercases the sport segment at line 257 so 'MLB-07-11-2026' and 'WC-07-11-2026' parse identically to lowercase; combined slug split at 258-261; split form via dateSeg; bare 'mlb'/'wc' returns isoDate:null at 264; junk sports rejected at 262; malformed dates rejected at 265-266; month 1-12 / day 1-31 range check at 269
- ✅ Hooks order intact: every hook (useLocation:290, useState theme:292, useEffect theme:302, canonicalize useEffect:316, useFeedCards:324 containing two useQuery at 803/813 and useMemo at 824) executes before the conditional early returns at DimeModelFeed.tsx:329-336 and 338-351 — no conditional hooks; Crest's useState (line 102) also precedes its null return
- ✅ Bare-sport canonicalization: needsDateCanonicalize (DimeModelFeed.tsx:315) drives navigate(feedModelPath(sport), { replace: true }) at line 318; wouter's navigate is useEvent-stable (node_modules/wouter/esm/index.js:95) so the effect deps [needsDateCanonicalize, sport, navigate] are safe; while pending, isoDate='' keeps both queries disabled (enabled: !!isoDate at DimeModelFeed.tsx:807/817) and renders an empty shell (329-336)
- ✅ go() routes exclusively through feedModelPath (DimeModelFeed.tsx:326-327), used by date arrows (377, 381) and sport chips (393, 401) — no path is hand-built anywhere in the page
- ✅ feedRoutes.ts helpers: toFeedSlugDate YYYY-MM-DD→MM-DD-YYYY (client/src/lib/feedRoutes.ts:21-24); feedModelPath lowercases sport and defaults to todayUTC() (31-34); todayUTC implements the 07:00 UTC rollover (client/src/components/CalendarPicker.tsx:115-125, FEED_CUTOFF_UTC_HOUR=7, MANUAL_WC_DATE_OVERRIDE=null); server feedSlugDate mirrors it exactly (server/_core/index.ts:756-769)
- ✅ legacyFeedRedirectTarget (client/src/lib/feedRoutes.ts:54-61): ?tab=splits → /betting-splits/MLB (56); ?sport=WC honored case-insensitively (57); valid ?date=YYYY-MM-DD carried into the slug, invalid format ignored → today (58-60); never emits a query string or legacy slug — wired at client/src/App.tsx:174 with replace
- ✅ Prefetch/query key parity: RequireAuth useFeedPrefetch prefetches wc2026.matchesByDate({date}) at client/src/components/RequireAuth.tsx:85 matching DimeModelFeed's useQuery({date: isoDate}) at DimeModelFeed.tsx:813-814, and games.list({sport:'MLB', gameDate}) at RequireAuth.tsx:87 matching useQuery({sport:'MLB', gameDate: isoDate}) at DimeModelFeed.tsx:803-804; staleTime 60s on both sides (RequireAuth.tsx:85,87 vs DimeModelFeed.tsx:809,819) so prefetched data is served without an immediate refetch; slug parsing at RequireAuth.tsx:73-82 handles combined, split, and bare forms with toLowerCase at line 74 (uppercase slugs prefetch correctly); router input schemas confirmed: games.list {sport?, gameDate?} (server/routers.ts:309-319), wc2026.matchesByDate {date} (server/wc2026/wc2026Router.ts:58-59)
- ✅ wouter <Redirect replace> honors replace: Redirect forwards its whole props object as navigation options (node_modules/wouter/esm/index.js:380-384), and all four legacy client redirects pass replace (client/src/App.tsx:146-148, 174)
- ✅ Full-page loads of canonical deep URLs survive: server 308 handler matches EXACT legacy paths only (server/_core/index.ts:771 app.get(['/feed','/splits','/projections','/dashboard'])) and the SPA catch-all serves index.html for /feed/model/* (server/_core/vite.ts:117-120)
- ✅ No legacy slug emissions remain in client code: repo-wide grep finds /feed, /splits, /projections, /dashboard only as redirect definitions (client/src/App.tsx:146-148,174) and comments; all 25+ nav call sites emit /feed/model/mlb or canonical paths (e.g. client/src/pages/Home.tsx:107,119, client/src/pages/UserManagement.tsx:686, client/src/features/mobileOwnerTabs/config.ts:30)
- ✅ Both test files pass against reality: npx vitest run → 28/28 (client/src/lib/feedRoutes.test.ts 10 tests, client/src/pages/dimeModelFeed.test.ts 18 tests); every source-regex assertion was cross-checked against the actual code lines (e.g. test line 129 ↔ DimeModelFeed.tsx:318, test line 84 ↔ DimeModelFeed.tsx:804)
- ✅ npx tsc --noEmit passes clean (exit 0) on the full working tree
- **[P3]** `client/src/App.tsx:112` — RootRoute's authenticated redirect to feedModelPath('MLB') uses push, not replace — pressing Back from the feed lands on '/' which immediately re-pushes forward, a soft back-button trap
  - *Disposition:* FIXED — RootRoute redirects use { replace: true }
  - navigate(target) at App.tsx:112 (and the pendingCheckout branch at :107) omit { replace: true }, unlike every legacy-slug redirect (App.tsx:146-148, 174). An authenticated user who loads '/' accumulates a '/' history entry they can never back through: Back → '/' → RootRoute effect → forward to the dated feed again. Contract only mandates the target (satisfied); this is a history-hygiene nit. Fix: navigate(target, { replace: true }).
- **[P3]** `client/src/pages/DimeModelFeed.tsx:269` — Calendar-invalid but format-valid slugs (e.g. /feed/model/mlb-02-31-2026) pass parseFeedModelPath, producing a URL/label/query three-way desync on crafted URLs
  - *Disposition:* ACKNOWLEDGED — crafted-URL only, recoverable via date arrows; not fixed
  - Line 269 validates only month 1-12 and day 1-31, so Feb 31 / Apr 31 etc. parse to isoDate '2026-02-31'. Verified on Node 22: new Date('2026-02-31T12:00:00Z') rolls over to Mar 3 (no crash), so prettyDate (DimeModelFeed.tsx:279-285) renders 'Tuesday, March 3' while games.list fires with gameDate '2026-02-31' (zodGameDate's Date.parse refine at server/securityMiddleware.ts:52-55 also accepts it via rollover; DB string-equality returns empty → 'No games for this date'), and the next-arrow shiftIso (273-277) jumps to 2026-03-04, skipping Mar 1-3. Recoverable via the arrows; crafted-URL only. Same gap exists in legacyFeedRedirectTarget's date regex (client/src/lib/feedRoutes.ts:59), which will happily emit the desynced slug from ?date=2026-02-31.
- **[P3]** `client/src/components/RequireAuth.tsx:67` — useFeedPrefetch's pathname gate is case-sensitive while wouter routes match case-insensitively, and junk sports trigger a useless MLB prefetch
  - *Disposition:* ACKNOWLEDGED — perf-only prefetch gap on uppercase URLs
  - pathname.startsWith('/feed/model') at RequireAuth.tsx:67 misses /FEED/MODEL/mlb-07-11-2026, which regexparam's 'i' flag routes to DimeModelFeed just fine — the page still fetches, only the prefetch optimization is skipped (perf-only). Separately, an unrecognized sport segment (e.g. /feed/model/nba) falls into the else branch at RequireAuth.tsx:84-88 and prefetches games.list {sport:'MLB', gameDate: today} even though DimeModelFeed renders the 'Invalid feed URL' page and never consumes it — one wasted request per crafted URL. Neither desyncs state nor emits a legacy slug.

### splits-routes — PASS

- ✅ /home/user/ai-sports-betting-dime-ai/client/src/App.tsx:182-187 — /betting-splits/:sport validates via parseSplitsSport; junk segment (e.g. /betting-splits/xyz) returns null and renders <Redirect to="/betting-splits/MLB" replace /> (wouter 3.7.1 Redirect supports the replace prop)
- ✅ /home/user/ai-sports-betting-dime-ai/client/src/App.tsx:188 — bare /betting-splits redirects to bettingSplitsPath("MLB") with replace; trailing-slash form /betting-splits/ also lands here because regexparam's :sport group ([^\/]+?) requires >=1 char
- ✅ /home/user/ai-sports-betting-dime-ai/client/src/lib/feedRoutes.ts:42-45 — parseSplitsSport uppercases the segment and accepts only MLB/NHL/NBA (case-insensitive: /betting-splits/mlb, /betting-splits/Nhl all resolve); wouter path matching itself is case-insensitive (regexparam 'i' flag), so /BETTING-SPLITS/nba also routes correctly
- ✅ /home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:178,185-190 — initialSport prop seeds selectedSport state; setSelectedSport wrapper (useCallback) calls setSelectedSportState(sport) + setLocation(bettingSplitsPath(sport), {replace:true}); useEffect at :190 re-syncs state whenever initialSport changes (covers external navigation, e.g. mobile Splits tab -> /betting-splits/MLB while on /betting-splits/NHL) — traced no desync path: route re-render passes the parsed sport back as initialSport and the effect is a no-op after pill clicks
- ✅ /home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:576,583,590 — ALL three setSelectedSport call sites (MLB/NHL/NBA pills) pass literal-union args; npx tsc --noEmit exits 0 (NODE_OPTIONS=--max-old-space-size=6144); no other setSelectedSport call sites exist in the file
- ✅ /home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:540 — AI MODEL PROJECTIONS tab links feedModelPath("MLB") which emits the dated canonical /feed/model/mlb-MM-DD-YYYY (feedRoutes.ts:31-34, todayUTC default); :551 — BETTING SPLITS self-link targets bettingSplitsPath(selectedSport), fixing the old /splits navigate-away bug (commit b17e369 diff confirms the old hrefs were /projections and /splits)
- ✅ grep sweep of client/src: zero href/navigate/setLocation/window.location targets to /splits, /projections, /dashboard, or non-model /feed — remaining string matches are internal card-tab state literals ('splits' in GameCard/MobileGameCard/useUrlState), comments, and the App.tsx:146-148 redirect definitions themselves; useUrlState (?tab= writer) is imported only by ModelProjections.tsx which App.tsx no longer routes or imports
- ✅ /home/user/ai-sports-betting-dime-ai/client/src/pages/SplitsLive.tsx — deleted (Glob: no file); only references left are the plan doc, and server/legacyRedirects.test.ts:50-52 which asserts App.tsx contains no SplitsLive
- ✅ MobileSplits still reachable and unaffected: App.tsx:219 (/m/:rest* -> MobileOwnerLayout) -> MobileOwnerLayout.tsx:23 (Route /m/splits -> MobileSplits); MobileSplits.tsx and MobileOwnerLayout.tsx are untouched by commit b17e369 and MobileSplits emits no navigation (grep: no navigate/setLocation/href/window.location)
- ✅ /home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/config.ts:31 — Splits tab path is /betting-splits/MLB; MobileOwnerBottomTabs.tsx:282 getActiveTab is pathname-only (/betting-splits* -> "splits"), and on /m/splits the fallback at :293-297 still lights the splits tab; tap handler (:146-148) uses SPA navigate, no full-page fallback
- ✅ /home/user/ai-sports-betting-dime-ai/server/_core/index.ts:771-787 — app.get(["/feed","/splits","/projections","/dashboard"]) issues HTTP 308: /splits and ?tab=splits -> /betting-splits/MLB (:774-775); exact-path match so /betting-splits/* and /feed/model/* never enter the handler
- ✅ Login round-trip preserves splits URLs: RequireAuth.tsx:128-134 redirects unauthenticated users to /login?returnPath=<encoded /betting-splits/{sport}>; Home.tsx:107,119 honors returnPath (default /feed/model/mlb per contract)
- ✅ Tests: npx vitest run client/src/lib/feedRoutes.test.ts server/legacyRedirects.test.ts server/mobileOwnerTabs.test.ts — 78/78 pass (bettingSplitsPath/parseSplitsSport/legacyFeedRedirectTarget unit-tested, feedRoutes.test.ts:29-47)
- **[P3]** `client/src/pages/BettingSplits.tsx:212` — Stale comments contradict the new auth/routing reality
  - *Disposition:* REPORTED — not fixed in this branch
  - Line 212 says 'No auth guard redirect — the page is fully public. Unauthenticated users can view splits.' but the route now wraps BettingSplits in RequireAuth (App.tsx:185), so unauthenticated users are redirected to /login before render. The plan doc (docs/plans/2026-07-11-navigation-reconstruction.md:133-134) confirms the auth change was explicitly requested, so behavior is intended — only the comment is wrong. Similarly the logout comment at :235-236 still claims 'App.tsx redirects / -> /feed'. Misleading for future editors; no runtime impact.
- **[P3]** `client/src/App.tsx:182` — Lowercase /betting-splits/mlb renders correctly but the URL is never case-canonicalized
  - *Disposition:* REPORTED — not fixed in this branch
  - parseSplitsSport uppercases the segment, so /betting-splits/mlb renders the MLB page, but unlike the feed's bare-slug history-replace canonicalization (DimeModelFeed.tsx:316-320), the address bar keeps the lowercase form until a sport pill is clicked. No app code emits the lowercase form and the contract only mandates case-insensitive acceptance, so this is cosmetic — but a shared lowercase link stays non-canonical.
- **[P3]** `client/src/pages/DimeModelFeed.tsx:388` — Tab-bar navigation is one-way: the feed surface has no link back to Betting Splits
  - *Disposition:* REPORTED — not fixed in this branch
  - BettingSplits.tsx:537-560 renders the two-tab bar (AI MODEL PROJECTIONS | BETTING SPLITS), but DimeModelFeed's header (dmf-sports at :388-405) offers only MLB/World Cup chips — no BETTING SPLITS entry point. Desktop users who follow the splits page's left tab to the feed can only get back via browser back, the Dime chat side-nav chip (DimeChatPage.tsx:50), mobile owner tabs, or a direct URL. Not a contract break (the plan never specs a splits link on the feed) and no legacy slug involved — flagged as a navigation asymmetry, adjacent to the splits area.

### legacy-redirects — PASS

- ✅ wouter 3.7.1 installed (node_modules/wouter/package.json) supports the `replace` prop on <Redirect>: RedirectProps includes HookNavigationOptions<BrowserLocationHook> (node_modules/wouter/types/index.d.ts:103-116), which resolves to { replace?: boolean; state?: S } (node_modules/wouter/types/use-browser-location.d.ts:19-22, 31-33). Runtime honored: Redirect passes its props object as navigate options (node_modules/wouter/esm/index.js:380-395) and navigate does history[replace ? 'replaceState' : 'pushState'] (node_modules/wouter/esm/use-browser-location.js:48-49).
- ✅ The (path, {replace}) signature on setLocation/navigate is supported (types/use-browser-location.d.ts:19-22) and used correctly for bare-slug canonicalization at client/src/pages/DimeModelFeed.tsx:318 (navigate(feedModelPath(sport), { replace: true })).
- ✅ All four client legacy routes are wouter <Redirect replace> into helper-built targets: /dashboard -> feedModelPath('MLB') (client/src/App.tsx:146), /projections -> feedModelPath('MLB') (App.tsx:147), /splits -> bettingSplitsPath('MLB') (App.tsx:148), /feed -> legacyFeedRedirectTarget(window.location.search) (App.tsx:174). Helpers verified pure and canonical-only (client/src/lib/feedRoutes.ts:31-61).
- ✅ window.location.search read in the /feed route render (App.tsx:174) is safe: the app is pure CSR — createRoot(...).render at client/src/main.tsx:322, no SSR/hydration of App; server-side, GET /feed is 308'd at server/_core/index.ts:771 before landingPrerenderMiddleware or any catch-all could render anything, and landingPrerender only special-cases exact /privacy, /terms, / (server/landingPrerender.ts:381-400). pushState is synchronous, so search is current at render time for SPA navigations.
- ✅ Server 308 handler (server/_core/index.ts:771-787) matches EXACT paths only — empirically probed against the installed Express 4.21.2 with the identical handler + catch-all ordering: /feed/model/mlb-07-11-2026, /feed/model/mlb, /feedx, /betting-splits/MLB all fell through to the SPA shell (no 308); /feed, /feed/, /FEED, /splits, /projections, /dashboard all 308'd to correct canonical targets.
- ✅ Handler registration order correct: app.get([legacy slugs]) at server/_core/index.ts:771 comes after every /api mount (tRPC at index.ts:731-743, rate limiters at 554-627, OAuth/Discord routes at 679-687) and before setupVite/serveStatic (index.ts:790-797), so it wins for full-page loads in both dev (vite.middlewares added inside setupVite at server/_core/vite.ts:43) and prod (catch-all at vite.ts:117-120).
- ✅ Query handling verified by probe: /feed?tab=splits -> 308 /betting-splits/MLB (also with &sport=WC — tab wins, matching client helper order at feedRoutes.ts:56); /feed?sport=WC and ?sport=wc -> /feed/model/wc-<today>; /feed?date=2026-07-04 -> /feed/model/mlb-07-04-2026; /feed?sport=WC&date=2026-07-04 -> /feed/model/wc-07-04-2026; malformed ?date=07-04-2026 falls back to today's slug. Logic at index.ts:772-784.
- ✅ 07:00 UTC rollover parity: server feedSlugDate (index.ts:756-769, FEED_CUTOFF_UTC_HOUR=7, minus 24h when getUTCHours()<7, getUTC* formatting) is line-for-line the same date math as client todayUTC (client/src/components/CalendarPicker.tsx:50, 115-129) which feedModelPath defaults to (feedRoutes.ts:31-34). Probe at 22:05 UTC 2026-07-11 produced 07-11-2026 on both sides.
- ✅ server/legacyRedirects.test.ts assertions are truthful: every regex was matched against the actual source lines (index.ts:771/786/774/775/783/760; App.tsx:146-148,174,179-182,185,110; Home.tsx:107,119) and the suite passes — 11/11, plus 10/10 in client/src/lib/feedRoutes.test.ts (vitest run, 21 passed).
- ✅ Redirect chain never dead-ends: unauthenticated full-page /feed -> 308 -> /feed/model/mlb-<date> -> RequireAuth hard-redirects to /login?returnPath=<canonical> (client/src/components/RequireAuth.tsx:128-134) -> Home setLocation(returnPath), default '/feed/model/mlb' (client/src/pages/Home.tsx:107-109,119,152). No legacy default anywhere in the chain.
- ✅ Client Switch ordering safe: wouter Route path='/feed' (App.tsx:174) is exact-match (regexparam, no nest prop), so /feed/model/... skips it and matches /feed/model/:sport/:date (App.tsx:179) or /feed/model/:sport (App.tsx:180); /betting-splits/:sport validates via parseSplitsSport with invalid sports Redirect-replaced to /betting-splits/MLB (App.tsx:182-188).
- ✅ Discord login/invite layers emit no legacy slugs: all error redirects go to '/' (server/discordLogin.ts:210-609, server/discordInvite.ts:172-543), invite success 302 -> /feed/model/mlb (server/discordInvite.ts:533) which the client canonicalizes to the dated URL via history-replace (DimeModelFeed.tsx:314-320).
- **[P2]** `server/discordAuth.ts:651` — The Discord account-LINKING flow still emits the legacy /dashboard slug in 12 live 302 redirects (?discord_linked=1 / ?discord_error=...), and the 308 layer strips those query params, silently swallowing the link success/error feedback.
  - *Disposition:* FIXED — same as above
  - res.redirect(302, "/dashboard?...") at discordAuth.ts:252, 345, 356, 371, 412, 424, 486, 533, 592, 639, 651, 659. These routes ARE registered (registerDiscordAuthRoutes at server/_core/index.ts:685). The contract says legacy slugs must NEVER be emitted by any redirect-default. Probe confirmed /dashboard?discord_linked=1 -> 308 /feed/model/mlb-07-11-2026 with the query dropped; the only consumer of discord_linked/discord_error toasts is the now-unrouted ModelProjections.tsx:470-514 anyway. Rated P2 not P1 because navigation never breaks (user lands on the canonical feed) and the only UI entry point to this flow (/api/auth/discord/connect link) lives on the unreachable ModelProjections page (ModelProjections.tsx:1072) — exposure is limited to direct URLs / stale bookmarks / in-flight OAuth callbacks. Fix: repoint the 12 targets to feedModelPath-equivalent (/feed/model/mlb?discord_linked=1) or /profile and rehome the toast handler.
- **[P3]** `server/email.ts:232` — Welcome-email CTA ('ENTER THE PLATFORM') links to https://aisportsbettingmodels.com/feed — a legacy slug emission by app code (also in the plain-text body at line 256).
  - *Disposition:* REPORTED — not fixed in this branch
  - Functionally this is rescued (arguably improved) by the 308: the link stays evergreen and resolves to the correct dated slug at click time. But it violates the letter of 'legacy slugs must never be emitted by any link', and it permanently pins the /feed 308 handler as load-bearing infrastructure for already-sent emails. If intentional, document it as the one sanctioned emitter; otherwise emit the undated canonical /feed/model/mlb (which DimeModelFeed canonicalizes client-side).
- **[P3]** `server/legacyRedirects.test.ts:44` — The 07:00-UTC-rollover assertion is a weak source-regex: it only asserts `const FEED_CUTOFF_UTC_HOUR = 7;` exists somewhere in index.ts, but index.ts also contains a second `FEED_CUTOFF_UTC_HOUR = 11` (games-cache pre-warm, server/_core/index.ts:947), so the test cannot detect the redirect handler being rewired to the wrong constant.
  - *Disposition:* FIXED — assertion scoped to the feedSlugDate helper
  - Truthful today — the redirect's own block-scoped const at index.ts:760 is 7 and is the one used at index.ts:762 (verified by reading). A behavioral test (supertest against a minimal app, or extracting feedSlugDate for direct unit-testing like the probe run here) would be a real guard; all other assertions in the file were verified to match the actual source lines and the suite passes 11/11.
- **[P3]** `client/src/components/CalendarPicker.tsx:113` — MANUAL_WC_DATE_OVERRIDE escape hatch (currently null) is client-only: if ever set, client todayUTC()/feedModelPath would pin one date while the server feedSlugDate (index.ts:756-769) keeps rolling — the 308 target date and client-side redirect/canonicalization targets would desync.
  - *Disposition:* REPORTED — not fixed in this branch
  - Currently null so full parity holds (verified identical math both sides). The comment at CalendarPicker.tsx:110-111 claims 'This is the ONLY place that needs to change for a manual date advance' — that is no longer true now that server/_core/index.ts has its own copy of the cutoff math for the 308 layer.
- **[P3]** `server/_core/index.ts:772` — Duplicate-param edge desync between the two redirect layers: /feed?tab=splits&tab=x gets req.query.tab as an array on the server (typeof check fails -> dated feed) but URLSearchParams.get('tab') returns 'splits' on the client (feedRoutes.ts:56 -> /betting-splits/MLB).
  - *Disposition:* REPORTED — not fixed in this branch
  - Contrived input; both outcomes are valid canonical pages, no 404 or loop, so impact is nil. Noted only for completeness — aligning would mean the server taking the first value (Array.isArray(tab) ? tab[0] : tab) or the client rejecting arrays.

### mobile-tabs — PASS

- ✅ config.ts tab paths are canonical with no '?' anywhere: /feed/model/mlb, /betting-splits/MLB, /chat, /m/props, /profile — client/src/features/mobileOwnerTabs/config.ts:29-35; test asserts exact array + no-'?' guard at server/mobileOwnerTabs.test.ts:33-49 (57/57 tests pass)
- ✅ getActiveTab: /feed/model/mlb-07-11-2026 and /feed/model/wc-07-11-2026 → feed via prefix match — client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:281
- ✅ getActiveTab: /betting-splits/MLB and /betting-splits/NHL → splits — client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:282; /m/props → props — MobileOwnerBottomTabs.tsx:283; /chat → chat and /profile → profile via exact-match loop — MobileOwnerBottomTabs.tsx:286-290
- ✅ Legacy owner routes /m/feed → feed and /m/splits → splits via /m/ segment fallback (t.id === segment) — client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:293-297
- ✅ getActiveTab is pathname-only: wouter 3.7.1 default location hook returns location.pathname (node_modules/wouter/esm/use-browser-location.js) and no useSearch/useUrlState import exists in client/src/features/mobileOwnerTabs/
- ✅ Tap handler navigates via wouter navigate(path) only — client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:148; zero window.location/href/pushState/location.assign in the feature directory (grep clean)
- ✅ Re-tapping active Feed from dated URL is harmless: navigate('/feed/model/mlb') → App.tsx:180 route → DimeModelFeed history-replaces to today's dated URL via navigate(feedModelPath(sport), {replace:true}) — client/src/pages/DimeModelFeed.tsx:316-320; no loop, no 404, no legacy slug emitted
- ✅ /m/props route exists and renders MobileBetTracker — client/src/features/mobileOwnerTabs/MobileOwnerLayout.tsx:25; single-segment /m/* matches App.tsx:219 /m/:rest* (verified by executing installed regexparam 3.0.0); unknown /m/xyz reaches inner catch-all redirect to /m/feed — MobileOwnerLayout.tsx:29
- ✅ GlobalMobileOwnerTabs mounts outside the route Switch so it renders on /feed/model/* and /betting-splits/* for owner mobile — client/src/App.tsx:236, gating at client/src/features/mobileOwnerTabs/GlobalMobileOwnerTabs.tsx:82-99
- ✅ MobileOwnerAccessGate renders nothing on denial (no redirect, no legacy slug emission) — client/src/features/mobileOwnerTabs/MobileOwnerAccessGate.tsx:44-47
- ✅ server/mobileOwnerTabs.test.ts would fail if a query hook were reintroduced in config.ts: exact path equality (lines 35-41) plus not.toContain('?') and legacy-slug regex (lines 44-49); suite ran green 57/57
- **[P2]** `client/src/features/mobileOwnerTabs/GlobalMobileOwnerTabs.tsx:90` — location.startsWith("/m") over-suppresses the global tabs: /mlb/team/:slug (App.tsx:202) starts with "/m", so an owner on mobile loses the bottom nav on MLB team schedule pages — contract says the only exclusion is /m/*
  - *Disposition:* FIXED — segment-exact /m check; /mlb/team/* keeps tabs
  - Should be location === "/m" || location.startsWith("/m/"). Same flawed predicate at line 107 (logging effect), and server/mobileOwnerTabs.test.ts:337-344 enshrines startsWith("/m") on string literals so no test catches it. Not a legacy-slug emission and the contract's named surfaces (/feed/model/*, /betting-splits/*) mount correctly, hence P2 not P1.
- **[P2]** `client/src/App.tsx:219` — Bare /m 404s: regexparam 3.0.0 compiles /m/:rest* to /^\/m\/([^/]+?)\/?$/i (exactly one segment), so /m and multi-segment /m/a/b fall through the Switch to NotFound (App.tsx:222), making MobileOwnerLayout.tsx:28's /m → /m/feed redirect unreachable dead code
  - *Disposition:* FIXED — explicit /m → /m/feed route added
  - Verified by executing the installed regexparam against the pattern; wouter 3.7.1's default parser is raw regexparam (wouter/esm/index.js:55) with no wildcard preprocessing. Nothing in client/src emits a link to bare /m (grep clean), so exposure is direct-URL/bookmark only — dead-end, not a legacy-slug leak. Fix: use wouter v3 wildcard syntax /m/*? at App.tsx:219.
- **[P3]** `client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:299` — getActiveTab defaults to "feed" for every unmatched path, so on /wc2026, /bet-tracker, /account, /resources (where global tabs render for owner mobile) the Feed tab is falsely lit with aria-selected=true
  - *Disposition:* REPORTED — not fixed in this branch
  - Minor active-state desync; returning null for unmatched paths would render no active tab instead of a wrong one.
- **[P3]** `client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:146` — Re-tapping the active Feed tab from a dated URL pushes /feed/model/mlb which DimeModelFeed then history-replaces to the same dated URL — one duplicate history entry per re-tap, so back button needs extra presses after repeated taps
  - *Disposition:* FIXED — active-tab re-tap navigates with replace
  - location !== path is true because tab.path (/feed/model/mlb) never equals the canonical dated pathname. Behavior is otherwise harmless (stays on feed tab, no loop). Could compare via getActiveTab(location) === tabId instead of string equality, or navigate with replace when the tab is already active.
- **[P3]** `server/mobileOwnerTabs.test.ts:346` — Test suite never imports MobileOwnerBottomTabs; getActiveTab is unexported and untested, so a query-string hook reintroduced in the component (rather than config.ts) would pass CI; two tests are expect(true).toBe(true) placeholders (lines 346-355)
  - *Disposition:* REPORTED — not fixed in this branch
  - The config-level guard (lines 33-49) is solid, but the component-level pathname-matching logic — including the /m/feed and /m/splits fallback traced in this audit — has zero test coverage. Export getActiveTab and add path-resolution tests.

### auth-login-flow — FAIL

- ✅ Home.tsx returnPath defaults: both call sites default to /feed/model/mlb — client/src/pages/Home.tsx:107 (already-authenticated redirect) and client/src/pages/Home.tsx:119 (form/Discord flow), Discord connect URL built with encodeURIComponent at client/src/pages/Home.tsx:120
- ✅ Discord login connect embeds returnPath in the signed state JWT (server/discordLogin.ts:192-194, :216, createStateToken at :90-99); callback verifies it (server/discordLogin.ts:112-116) and 302s to returnPath at server/discordLogin.ts:575 — /feed/model/wc-07-11-2026 round-trips intact end to end
- ✅ RequireAuth preserves returnPath for unauthenticated hits: loginUrl = /login?returnPath=encodeURIComponent(pathname+search) at client/src/components/RequireAuth.tsx:128-134; Home decodes via URLSearchParams and re-encodes into the connect URL, so the query-free dated WC path survives the full login loop
- ✅ server/discordInvite.ts:533 redirects invite-flow success to /feed/model/mlb (canonical, self-canonicalizing)
- ✅ RootRoute authenticated redirect uses feedModelPath("MLB") at client/src/App.tsx:110-112; pendingCheckout path navigates to /checkout?plan=... first and returns (client/src/App.tsx:103-108) — unaffected
- ✅ Legacy slugs exist only as redirect consumers: client-side wouter <Redirect replace> at client/src/App.tsx:146 (/dashboard), :147 (/projections), :148 (/splits), :174 (/feed with legacyFeedRedirectTarget(window.location.search)); server 308 handler at server/_core/index.ts:771-787 maps /splits and ?tab=splits → /betting-splits/MLB (:774-775), honors ?sport=WC (:777-778) and legacy ?date (:779-782)
- ✅ Server 308 date cutoff parity: feedSlugDate uses FEED_CUTOFF_UTC_HOUR=7 (server/_core/index.ts:760) matching client todayUTC (client/src/components/CalendarPicker.tsx:50)
- ✅ Canonical feed routes registered: /feed/model/:sport/:date and /feed/model/:sport wrapped in RequireAuth (client/src/App.tsx:179-180); /betting-splits/:sport validates via parseSplitsSport with invalid-sport fallback redirect, plus bare /betting-splits redirect (client/src/App.tsx:182-188)
- ✅ DimeModelFeed slug parsing is case-insensitive (lowercases sport segment, client/src/pages/DimeModelFeed.tsx:256-262); bare /feed/model/{sport} canonicalizes to today's dated URL via navigate(feedModelPath(sport), {replace:true}) — history-replace, back-button safe (client/src/pages/DimeModelFeed.tsx:315-320); invalid slugs render an explicit invalid-URL state, no dead redirect loop (client/src/pages/DimeModelFeed.tsx:338-351)
- ✅ Mobile owner tabs match contract exactly: Feed→/feed/model/mlb, Splits→/betting-splits/MLB, Chat→/chat, Props→/m/props, Profile→/profile (client/src/features/mobileOwnerTabs/config.ts:30-34); getActiveTab is pathname-only with no query parsing (client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:278-300); /m/props is routed (client/src/features/mobileOwnerTabs/MobileOwnerLayout.tsx:25)
- ✅ BettingSplits seeds sport from initialSport route prop and syncs URL on pill change via setLocation(bettingSplitsPath(sport), {replace:true}) (client/src/pages/BettingSplits.tsx:178-190); in-page splits link uses bettingSplitsPath(selectedSport) (client/src/pages/BettingSplits.tsx:551)
- ✅ Every remaining client navigation hook emits canonical targets only — full-repo literal sweep of client/src for /feed, /feed?, /splits, /projections, /dashboard found emitters exclusively targeting /feed/model/mlb: MlbTeamSchedule.tsx:569, ModelResults.tsx:444, IngestAnOdds.tsx:83, ClaudeAssistant.tsx:99, ManageAccount.tsx:157, TheModelResults.tsx:1012, UserManagement.tsx:686/:814, PublishProjections.tsx:1631/:1916, SubscribeSuccess.tsx:237, DimeChatPage.tsx:49/:489, main.tsx (comment only); all other hits are comments, tests, feedRoutes.ts itself, or the App.tsx redirect consumers
- ✅ Logout targets are clean: Profile.tsx:69 → "/", BettingSplits.tsx:237 → "/login", ManageAccount logout → "/" (ManageAccount.tsx:52); LoginModal returnPath = current pathname (client/src/components/LoginModal.tsx:47-50)
- ✅ Legacy query-tab page ModelProjections.tsx is fully unrouted — no import of it anywhere in client/src (App.tsx now lazy-loads DimeModelFeed at client/src/App.tsx:25), so its /api/auth/discord/connect link (ModelProjections.tsx:1072) and discord_linked handling are unreachable dead code
- **[P1]** `server/email.ts:232` — Live welcome email emits legacy slug /feed as its CTA link — contract forbids any link emitting legacy slugs
  - *Disposition:* FIXED — CTA now links /feed/model/mlb (HTML + plain-text)
  - The 'ENTER THE PLATFORM' button (server/email.ts:232) and the plain-text body (server/email.ts:256) both link to https://aisportsbettingmodels.com/feed. sendWelcomeEmail is live: fired from the Stripe checkout webhook at server/routers/stripe.ts:690-691, so every new subscriber receives a legacy-slug link. It functionally resolves via the 308 handler, but the contract states legacy slugs must NEVER be emitted by any link — only consumed by redirects. Fix: link to /feed/model/mlb (self-canonicalizing, date-safe for emails opened days later).
- **[P1]** `server/discordAuth.ts:252` — Live Discord account-linking route emits legacy slug /dashboard in 13 redirect targets; the 308 handler then silently strips the discord_linked/discord_error query state
  - *Disposition:* FIXED — all redirects → /feed/model/mlb?discord_…; 308 layer now forwards unconsumed query params; DimeModelFeed surfaces the toast
  - server/discordAuth.ts redirects to /dashboard?discord_error=... at lines 252, 345, 356, 371, 412, 424, 486, 533, 592, 639, 659 and to /dashboard?discord_linked=1 at line 651. registerDiscordAuthRoutes is registered live at server/_core/index.ts:685 (prefix /api/auth/discord, discordAuth.ts:70). Any hit (old bookmark, cached page, direct URL) completes OAuth then 302s to /dashboard, which the 308 handler (server/_core/index.ts:771-786) rewrites to the dated feed slug WITHOUT preserving the query string — so both the legacy slug emission and all link-result feedback (already_linked, db_write_failed, success) are emitted then swallowed. The only client entry (ModelProjections.tsx:1072) is dead code, so either retire these routes or repoint their redirects to canonical paths.
- **[P2]** `server/_core/index.ts:786` — 308 permanent redirect to a DATE-VARYING target with no Cache-Control — browsers may cache /feed → /feed/model/mlb-07-11-2026 indefinitely, breaking the 07:00 UTC rollover for repeat visitors
  - *Disposition:* FIXED — Cache-Control: no-store on the 308
  - res.redirect(308, target) at server/_core/index.ts:786 sets no Cache-Control, and 308 responses are cacheable by default (RFC 9110; Chrome caches permanent redirects aggressively). The target embeds today's date (server/_core/index.ts:783), so a user who hits /feed (e.g. from the welcome email) gets the redirect pinned to that day's slug; on later visits the browser replays the cached 308 and lands them on a stale slate, contradicting the contract's 'dated feed slug (07:00 UTC rollover)' requirement. /splits → /betting-splits/MLB is stable and safe. Fix: add Cache-Control: no-store to the dated redirects, or use 302/307 for /feed//projections//dashboard while keeping 308 for /splits.
- **[P2]** `server/discordLogin.ts:293` — All Discord OAuth error redirects target '/' where nothing consumes ?discord_error — the login page's error banner and auto-retry (Home.tsx) are unreachable, so login failures produce zero user feedback
  - *Disposition:* FIXED — errors now land on /login where the banner/auto-retry live
  - Every error branch in server/discordLogin.ts (lines 273-609: state_expired, no_account, account_expired, timeout, db_unavailable, etc.) and server/discordInvite.ts (lines 172-543) 302s to /?discord_error=.... Since the 2026-07-09 root swap, '/' renders DimeLandingV2 (client/src/App.tsx:140), which never reads discord_error (grep: only client/src/pages/Home.tsx:117 at /login and the unrouted ModelProjections.tsx consume it). A user whose Discord sign-in fails silently lands on the marketing page; Home's DISCORD_ERRORS map (Home.tsx:42-64), error banner (Home.tsx:388-410), and transient auto-retry (Home.tsx:123-140) are dead for the OAuth callback path. Pre-existing before this branch, but it dead-ends the traced login loop's failure half. Fix: redirect errors to /login?discord_error=... 
- **[P2]** `server/discordLogin.ts:575` — Open redirect: the OAuth callback 302s to the JWT-carried returnPath with no validation that it is a same-site relative path
  - *Disposition:* FIXED — sanitizeReturnPath() at connect + JWT-verify (relative same-site paths only)
  - returnPath flows attacker-controlled from /login?returnPath=https%3A%2F%2Fevil.com → Home.tsx:120 → /connect (server/discordLogin.ts:192-194, no validation) → state JWT → res.redirect(302, returnPath) at server/discordLogin.ts:575. Express redirects to absolute and protocol-relative (//evil.com) URLs, so a successful Discord login can bounce the victim to an attacker site — classic OAuth open-redirect phishing. The client side is incidentally guarded (wouter pushState throws cross-origin) but the server hard-redirect is not. Fix: reject returnPath unless it matches /^\/(?!\/)/ before signing it into the state token or before redirecting.
- **[P3]** `client/src/main.tsx:230` — Session-expiry handler redirects to '/' (landing) not /login and drops the current path — toast promises 'Redirecting to login' but the user lands on marketing with no returnPath
  - *Disposition:* FIXED — /login with returnPath preserved
  - redirectToLoginIfUnauthorized (client/src/main.tsx:194-232) shows a 'Redirecting to login...' toast then sets window.location.href = "/" at line 230. The expired user sees the landing page, must click Sign In manually, and their in-app location is lost (no returnPath). Not a legacy-slug emission, but a returnPath-preservation gap in the auth loop; RequireAuth-gated pages handle the same case correctly (RequireAuth.tsx:128-134). Fix: redirect to /login?returnPath=<pathname+search>.
- **[P3]** `client/src/pages/ModelProjections.tsx:1072` — Unrouted dead page still ships the only UI entry to the /dashboard-emitting discordAuth link flow and stale legacy-slug comments — delete alongside retiring discordAuth
  - *Disposition:* REPORTED — not fixed in this branch
  - ModelProjections.tsx is no longer imported anywhere (App.tsx routes DimeModelFeed instead), but it remains in the bundle source with href=/api/auth/discord/connect (line 1072), discord_linked/discord_error URL handling (lines 470-514), and legacy '/feed' flow comments (line 545). Keeping it invites re-wiring the legacy loop; removing it (plus server/discordAuth.ts per the P1) closes the /dashboard emission class permanently.

## UI/UX audit findings

### Lens: mobile

**Strengths:**
- DimeModelFeed's hit-slop engineering is genuinely good: ::after{inset:-8px} on the 28px date arrows lands exactly 44px effective targets, and ::after{inset:-6px} on the sport chips lands ~44px — touch-target math done deliberately, not by accident (DMF_CSS lines 878-879, 885).
- The DMF container-query ladder (1000 → 700 → 440) delivers the owner rules on phones: every market keeps both sides as rows at all widths, labels stack above values below 440 so odds never truncate at 360px, and the 2-col market grid with span rules for odd columns is clean (lines 961-991).
- Correct mobile scroll architecture on the feed: min-height:100vh + 100dvh override, inner .dmf-scroll{flex:1;overflow-y:auto}, so the sticky .dmf-feedhead actually sticks inside the real scrollport instead of the common broken sticky-inside-non-scrolling-ancestor pattern (lines 846, 874-876).
- Rare accessibility polish in DMF_CSS: both prefers-reduced-motion (kills transitions AND the live-dot pulse, exactly what MASTER.md demands) and prefers-contrast:more with per-theme token overrides (lines 992-999) — almost no production code ships the latter.
- The bottom tab bar's core dimensions meet spec: 60px bar, minWidth/minHeight 44px per button, env(safe-area-inset-bottom) padding on the nav itself, 11px labels at the legibility floor, aria-label per tab, and pathname-prefix active matching that keeps Feed lit across dated /feed/model/* URLs (MobileOwnerBottomTabs.tsx:176, 215-216, 281-283).
- ViewportGate's listener is the right primitive: matchMedia('(max-width:1023px)') change events with an SSR-safe lazy initializer — no resize-thrash, no layout reads (DimeChatPage.tsx:542-569).
- DMF press feedback on the date arrows (.dmf-sq:active{background:var(--dmf-card-hi)}, line 881) plus the single 160ms cubic-bezier(.16,1,.3,1) curve everywhere — motion discipline matches MASTER.md's 2/10 dial exactly.
- Responsible-gaming line ('21+ · Gambling problem? Call 1-800-GAMBLER') is present on the feed and stays visible above the tab bar thanks to the mobile bottom padding (DimeModelFeed.tsx:425, DMF_CSS 875).
- BettingSplits search results deliberately switch to official team abbreviations under sm: with nowrap so names never ellipsize on phones (BettingSplits.tsx:148, 164) — the zero-truncation ethos applied to search.
- Navigation contract compliance is clean on all mobile-touched surfaces: tab config, gate link, splits tab bar, and chat sidebar all emit only canonical /feed/model/* and /betting-splits/* paths (config.ts:29-35, DimeChatPage.tsx:45-53, BettingSplits.tsx:540-551), and DMF's bare-sport canonicalization uses replace so back never lands on the dateless form (DimeModelFeed.tsx:313-320).

- **[P0 | mobile]** `client/src/pages/BettingSplits.tsx:644` — Last game card is permanently occluded by the fixed bottom tab bar — main has only pb-1 (4px) while the owner tab bar covers 60px + env(safe-area-inset-bottom) (up to ~94px on notched iPhones)
  - *Disposition:* FIXED — body.mobile-owner-tabs-active reserves real flow space (fixed no-op spacer removed)
  - *Suggested fix:* In BettingSplits.tsx line 644 change `<main className="w-full pb-1">` to `<main className="w-full pb-1 feed-pb-safe">`. Separately, delete the fixed 'spacer' div in GlobalMobileOwnerTabs.tsx:148-154 (it does nothing) or replace it with a comment pointing to the body-class + .feed-pb-safe mechanism so future pages don't trust it.
- **[P1 | mobile]** `client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:173` — Tab bar zIndex 9999 renders above and stays tappable over AgeModal/LoginModal (both z-50) — the 21+ age-acknowledgment gate can be visually overlapped and routed around
  - *Disposition:* FIXED — tab bar z-index 40, below the z-50 AgeModal/LoginModal
  - *Suggested fix:* Lower the nav below modal layer: `zIndex: 40` in MobileOwnerBottomTabs.tsx:173 (the splits sticky header is z-40 but occupies the top edge — no overlap; its dropdown is z-50). Alternatively raise both modals to z-[10000], but lowering the nav is the systemic fix: page chrome should never outrank blocking dialogs.
- **[P1 | mobile]** `client/src/pages/BettingSplits.tsx:601` — Search input font is 12px (text-xs) — iOS Safari auto-zooms the whole page ~133% on focus and does not restore it, breaking the sticky header layout
  - *Disposition:* FIXED — 16px on touch widths (text-base sm:text-xs)
  - *Suggested fix:* Set the input to 16px on mobile: replace `className="flex-1 min-w-0 bg-transparent text-xs …"` with `text-base sm:text-xs` (16px under sm, 12px above where iOS zoom doesn't apply), and bump the container's py-1.5 to keep vertical rhythm.
- **[P1 | mobile]** `client/src/pages/dime-chat/DimeChatPage.tsx:489` — The ViewportGate's only in-page action — the escape link to /feed/model/mlb — is a 10px IBM Plex Mono text link with a ~12px-tall hit area (spec: ≥44px)
  - *Disposition:* FIXED — 44px min-height inline-flex hit area on the gate link (conversation.css)
  - *Suggested fix:* Give the link a real target: in conversation.css add `.dc-gate-sub{display:inline-flex;align-items:center;justify-content:center;min-height:44px;padding:0 20px;border:1px solid var(--color-border);border-radius:20px;}` (brand pill spec, MASTER.md Buttons/pills). Consider copy 'Open AI Model Projections' with the mono micro-label reserved for the secondary line.
- **[P1 | mobile]** `client/src/pages/DimeModelFeed.tsx:878` — No touch-action:manipulation anywhere in DMF_CSS — rapidly tapping the ‹ › date arrows (the primary paging gesture) triggers iOS double-tap page zoom
  - *Disposition:* FIXED — applied to all DMF buttons/links
  - *Suggested fix:* Add one line to DMF_CSS near the button reset (line 858): `.dmf-root :where(button){…;touch-action:manipulation}` — or broader: `.dmf-root :is(.dmf-sq,.dmf-chip,.dmf-themebtn){touch-action:manipulation}`.
- **[P1 | mobile]** `client/src/pages/DimeModelFeed.tsx:877` — At 320–350px the date-nav row exceeds available width; the 28px arrow buttons are the only shrinkable items and compress below their 28px spec (hit area drops under 44px), or the row overflows horizontally
  - *Disposition:* FIXED — wrap + tightened gaps ≤440px; arrows keep 44px effective hit area
  - *Suggested fix:* In DMF_CSS: (1) `.dmf-sq{flex-shrink:0}` so arrows never compress; (2) shorten the date under the 440 container query — either `@container dmf (max-width:440px){.dmf-datelbl{font-size:14px}}` plus switching prettyDate to `weekday:"short", month:"short"` below 440 via a matchMedia/container check, or simply drop `white-space:nowrap` on .dmf-slatecount's row and let the count wrap under the date; (3) add `overflow-x:hidden` to .dmf-scroll as a belt.
- **[P2 | mobile]** `client/src/pages/DimeModelFeed.tsx:928` — Market footer edge lines ('MIL +1.5 · +10.5%', 11px mono, white-space:nowrap) clip inside overflow:hidden .dmf-mkbox below ~345px — the edge percentage digits are cut off
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Under the 440 container query add: `.dmf-mkfoot{white-space:normal;line-height:1.35;padding:4px 4px}` (two-line wrap keeps every character) — or keep nowrap but drop to `font-size:10px;letter-spacing:.02em` below 360: `@container dmf (max-width:360px){.dmf-mkfoot.dmf-edge{font-size:10px}}`.
- **[P2 | mobile]** `client/src/pages/DimeModelFeed.tsx:875` — Bottom clearance is a viewport media query (max-width:767px, 130px magic number) while the tab bar mounts at innerWidth <= 768 and only for authenticated owners — 1px mismatch band plus 130px dead padding for every non-owner mobile user
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Replace the media query in DMF_CSS with a body-class hook that inherits into the scoped sheet: `body.mobile-owner-tabs-active .dmf-scroll{padding-bottom:calc(76px + env(safe-area-inset-bottom,0px))}` and keep a small default `padding-bottom:60px`. This kills the 767/768 seam, the non-owner dead space, and the magic number in one move.
- **[P2 | mobile]** `client/src/pages/DimeModelFeed.tsx:810` — Date paging shows the previous day's games under the new date header with zero fetching indicator — placeholderData keeps stale cards and only isLoading (not isFetching/isPlaceholderData) gates the skeletons
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Surface fetch state: return `isFetching: isWc ? wcQuery.isFetching : mlbQuery.isFetching` from useFeedCards, then render `<div className="dmf-list" style={{opacity: isFetching ? 0.55 : 1, transition: "opacity 160ms cubic-bezier(.16,1,.3,1)"}}>` plus a mono 'LOADING…' micro-label in .dmf-slatecount while isFetching. Cheap, brand-legal (opacity de-emphasis is the house pattern for non-signal).
- **[P2 | mobile]** `client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:222` — Tab taps give no visual press feedback: WebkitTapHighlightColor is suppressed and there is no :active/pointer-down state — only a transition declaration with nothing to transition on press
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add a tiny scoped stylesheet (or module class) for the tab buttons: `.mot-tab:active{transform:scale(0.94);opacity:.8;transition:transform 100ms ease-out}` and put className="mot-tab" on the button; or handle onPointerDown/onPointerUp to set a pressed state that drives the existing inline transform.
- **[P2 | mobile]** `client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:299` — getActiveTab falls back to "feed" for any unmatched pathname — the Feed tab lights up while the user is on /bet-tracker, /admin/*, /profile-adjacent routes, lying about location
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Return `null` at line 299 instead of "feed". The render path already handles activeTabId === null (isActive simply false for all tabs). Keep the explicit prefix matches (lines 281-283) as-is — they are correct.
- **[P2 | mobile]** `client/src/pages/BettingSplits.tsx:541` — Header tab label 'AI MODEL PROJECTIONS' (20 chars, 14px bold, tracking-wide + 14px icon) exceeds its 50% column below ~400px and wraps to two lines, producing a jagged two-height tab bar
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add responsive sizing on both tab spans: `className="… text-xs min-[430px]:text-sm"` with `whiteSpace:'nowrap'` on the span, or shorten the mobile label ('AI PROJECTIONS') under 400px. Verify at 320: 2×(icon 14 + gap 8 + 13 chars @ 12px bold ≈ 120px) fits 160px columns.
- **[P2 | mobile]** `client/src/pages/BettingSplits.tsx:460` — Multiple sub-44px touch targets in the splits header: user-menu button 28px (w-7 h-7), sport pills ~34px tall, calendar trigger ~30px, calendar month arrows 24px (w-6 h-6), day cells ~31px
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* User menu: `w-11 h-11 -m-2` (44px hit, same 28px visual via inner span) or add `before:absolute before:-inset-2` with relative. Pills: `py-2.5` (44px) or relative + `after:absolute after:-inset-1.5`. CalendarPicker: widen popup to 280px (7×40px cells), bump arrows to `w-10 h-10 -m-2`, trigger to `py-2`.
- **[P2 | mobile]** `client/src/pages/BettingSplits.tsx:563` — Row 3 filter bar cannot fit at 320px: CalendarPicker + three flex-shrink-0 sport pills consume ~300px, collapsing the flex-1 search field to near-zero width
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Let the row wrap on small screens: add `flex-wrap` to the row 3 container so search drops to its own full-width line below the pills (`<div className="relative px-3 pt-1 pb-1 flex items-center gap-2 flex-wrap">` + `basis-full min-[430px]:basis-auto` on the search wrapper), and keep the dropdown anchored to the row container (already `left-3 right-3 top-full`).
- **[P2 | mobile]** `client/src/pages/BettingSplits.tsx:449` — Sticky header stacks four rows (~150-170px) and combines with the 60-94px bottom tab bar to consume ~40-45% of a 320×568 viewport, leaving roughly one game card of scroll window
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Keep rows 2-3 sticky and let row 1 (brand) + row 4 (date header) scroll away: split the header into a non-sticky brand block and a sticky nav block, or add a scroll listener/IntersectionObserver toggling a `.compact` class that hides row 1 and row 4 after 40px of scroll (transition 160ms per brand curve).
- **[P2 | mobile]** `client/src/pages/dime-chat/frozen-tokens.css:62` — ViewportGate escape link uses .dc-link (#45E0A8) in light theme — raw mint text on white is ~1.9:1 contrast, explicitly banned by MASTER.md ('--mint-on-light #0FA36B for all mint text on light')
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add a theme-scoped override next to the frozen rule: `.theme-light .dc-link{color:var(--mint-on-light)}` and `.theme-light .dc-link:hover{color:#0C8A5B}` — or scope it to the gate only if the frozen chat shell must stay verbatim: `.theme-light .dc-gate-sub.dc-link{color:var(--mint-on-light)}`.
- **[P2 | mobile]** `client/src/pages/BettingSplits.tsx:540` — Both header tabs nest a <button> inside a wouter <Link> (renders <a>) — invalid HTML with two focusable/tappable elements per tab and inconsistent tap behavior across mobile browsers
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Drop the inner button: put the classes/styles on the Link itself (`<Link href={…} className="flex-1 flex items-center justify-center gap-2 py-2.5 …">`). For the active self-link, intercept with onClick={e => e.preventDefault()} or render a <span aria-current="page"> instead of a link.
- **[P2 | mobile]** `client/src/pages/DimeModelFeed.tsx:982` — Model win% annotation (.dmf-wp) is display:none below the 700px container — phones never see the model's win probability, which the page spec lists as a required annotation
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Instead of hiding, relocate at ≤440: in the 440 container block add `.dmf-rlab .dmf-wp{display:inline;margin-left:auto}` — or keep it in the model cell but only on the ML column. Between 440-700 (rare phone-landscape), keep it visible at 10px if space is the concern. Bump base size to 12px per page spec.
- **[P2 | mobile]** `client/src/pages/DimeModelFeed.tsx:870` — Theme toggle effective touch target is ~42×58px — 26px visual height + 8px hit-slop falls just under the 44px minimum
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Change line 871 to `.dmf-themebtn::after{content:"";position:absolute;inset:-10px}` (26 + 20 = 46px), or raise padding to 8px 12px.
- **[P3 | mobile]** `client/src/pages/DimeModelFeed.tsx:922` — Side labels (.dmf-lab, 10px mono) carry live line values ('NYY -1.5', 'O 8.5') — data at 10px on phone screens is below the lens's 11px data floor
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* In the 440 container block: `.dmf-lab{font-size:11px}` (labels-with-values only get the bump where they own a full row).
- **[P3 | mobile]** `client/src/pages/DimeModelFeed.tsx:859` — The shared focus-visible rule forces border-radius:8px, reshaping the 18px-radius sport chips when focused (visible with keyboard/switch access on mobile too)
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Remove `border-radius:8px` from the shared rule and let each control keep its own radius; the box-shadow ring follows border-radius automatically.
- **[P3 | mobile]** `client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:178` — role="tablist"/role="tab" on a navigation bar — screen-reader users are promised tab-panel semantics (arrow-key switching, tabpanel) that don't exist; #39FF14 active color also conflicts with brand law
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Use `<nav aria-label="Main">` with plain buttons/links carrying `aria-current="page"` on the active item; drop role=tablist/tab/aria-selected. Raise the #39FF14 vs mint #45E0A8 conflict with the owner before touching COLORS.
- **[P3 | mobile]** `client/src/pages/BettingSplits.tsx:429` — scrollToGame highlight pulse runs a 300ms setInterval flash loop with hardcoded green #22c55e and ignores prefers-reduced-motion
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Gate the pulse behind `!window.matchMedia('(prefers-reduced-motion: reduce)').matches` (fall back to a single static 2px outline for 1.5s) and use the page's accent token instead of #22c55e.
- **[P3 | mobile]** `client/src/pages/BettingSplits.tsx:198` — headerHeight state + ResizeObserver are dead code — measured on every header resize, never read in render
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Delete headerRef's ResizeObserver effect and the headerHeight state (keep the ref only if something else needs it — nothing does).
- **[P3 | mobile]** `client/src/pages/DimeModelFeed.tsx:961` — The entire responsive ladder is @container queries with no @media fallback — browsers without container-query support (iOS Safari ≤15.x) get the desktop 250px/1fr/240px grid crushed into 320px
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Since the container equals the viewport here, mirror the three blocks as equivalent @media (max-width:1000px/700px/440px) rules placed BEFORE the @container blocks (supporting browsers apply both identically; legacy browsers at least get the media path). Or accept and document the floor: iOS 16+.

### Lens: tablet

**Strengths:**
- Container-query architecture (DMF_CSS: container-type:inline-size on .dmf-root, breaks at 1000/700/440) is the right tool — card reflow is driven by available width, not viewport guesses, so a future sidebar won't break the grid.
- The 769–1000px stacked mk3 layout itself is genuinely good tablet design: matchup header, 3-across market boxes, verdict strip promoted to a full-width mint-disciplined summary bar — density 8/10 per MASTER with no truncation.
- Touch-first hit-area engineering on the feed controls: ::after{inset:-8px} extensions bring the 28px date arrows and theme button to ~44pt, chips get inset:-6px, and .dmf-sq has a real :active state — MASTER's 'hover-only affordances' anti-pattern is properly addressed (DimeModelFeed.tsx:871,879,885).
- Brand law is executed with discipline in DMF_CSS: one-accent mint strictly on signal (sig values, edge footers, live dot, active states), PASS cards at 0.82 opacity with grey verdicts, #0FA36B swap + coin-dot/live-dot hairline keylines in the light theme, IBM Plex Mono micro-labels at 10px/0.08em, single 160ms cubic-bezier(.16,1,.3,1) curve throughout.
- Accessibility media queries beyond the minimum: prefers-reduced-motion kills transitions and the live pulse (DimeModelFeed.tsx:992-995), and prefers-contrast:more re-tokens borders and text tiers for both themes (:996-999) — rare to see the contrast query handled at all.
- Route canonicalization is history-safe: bare /feed/model/{sport} replaces (not pushes) to the dated URL (DimeModelFeed.tsx:315-320), and BettingSplits syncs sport-pill changes to the URL with replace (BettingSplits.tsx:186-189) — back-button behavior and shareability both survive (ui-ux-pro-max §9 deep-linking/back-behavior).
- Loading and date-paging avoid layout collapse: skeleton rows while empty, placeholderData/keepPreviousData holds the previous slate during date changes with 60s polling per the data contract (DimeModelFeed.tsx:798-831).
- MobileOwnerBottomTabs meets the spec it documents: 44px minimum targets, role=tablist/tab with aria-selected and labels, safe-area-inset-bottom padding, haptic tap, active dot + weight shift — and getActiveTab's pathname-prefix matching keeps the Feed tab lit across any dated slug (MobileOwnerBottomTabs.tsx:278-300).
- DimeChatPage's viewport gate is placed after all hooks so an in-flight SSE stream survives a portrait↔landscape rotation through the gate and back (DimeChatPage.tsx:794-798) — thoughtful orientation handling; the gate itself is static, brand-correct, and reduced-motion-safe.

- **[P0 | tablet]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:357` — Navigation dead-zone at 769–1023px: DimeModelFeed has zero outbound navigation, mobile tabs are gone (>768), and /chat is gated (<1024) — a tablet user landing on the canonical feed cannot reach Splits, Chat, or Profile at all.
  - *Disposition:* FIXED — Splits/Chat/Profile links added to the DMF topbar
  - *Suggested fix:* Implement the mandated shell: add the Dime sidebar (sidebar-row spec, MASTER.md:117-128) to DimeModelFeed at ≥1024, and for 768–1023 render a compact nav row in .dmf-topbar — e.g. after .dmf-toptitle: <nav className="dmf-topnav"><Link href={bettingSplitsPath("MLB")}>Betting Splits</Link><Link href="/chat">Chat</Link><Link href="/profile">Profile</Link></nav> styled as .dmf-chip pills. Cheapest interim: change GlobalMobileOwnerTabs.tsx:74 to window.innerWidth < 1024 so the bottom tab bar covers the exact range where the chat gate applies (one seam instead of two), and add the matching bottom clearance (see the 767/768 seam finding).
- **[P0 | tablet]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:961` — MLB (mk3) cards switch to the 250px|1fr|240px desktop grid at container >1000px, but at 1001–1200px the three market columns compute to ~17–43px value tracks vs ~36–64px nowrap content — Book/Model odds visually overlap on every iPad landscape (1024/1080/1180/1194).
  - *Disposition:* FIXED — stack threshold raised 1000→1200px container
  - *Suggested fix:* In DMF_CSS change line 961 from `@container dmf (max-width: 1000px)` to `@container dmf (max-width: 1199px)` so mk3 keeps the stacked matchup-header + 3-across-markets layout through iPad landscape. Optionally also soften the fixed rails for 1200–1300: `.dmf-gbody{grid-template-columns:minmax(210px,250px) 1fr minmax(200px,240px)}`.
- **[P1 | tablet]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:953` — World Cup (mk7) cards keep the 4-across market grid all the way down to 701px container — at iPad portrait (768–834) each market column is ~172–188px, leaving ~28–36px value tracks, and the ML column's win% annotation (~62px needed) overlaps the Book column.
  - *Disposition:* FIXED — mk7 drops to 2×2 at ≤900px container
  - *Suggested fix:* Add a mid step: `@container dmf (max-width: 1000px){ .dmf-game.dmf-mk7 .dmf-markets{grid-template-columns:repeat(2,1fr)} .dmf-game.dmf-mk7 .dmf-mkcol:nth-child(4n){border-right:1px solid var(--dmf-border)} .dmf-game.dmf-mk7 .dmf-mkcol:nth-child(2n){border-right:0} .dmf-game.dmf-mk7 .dmf-mkcol:last-child:nth-child(odd){grid-column:1/-1} .dmf-wp{display:none} }` (i.e. move both the 2-col fallback and the .dmf-wp cutoff for mk7 from 700 up to 1000, keeping the existing nth-child border corrections consistent).
- **[P1 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:846` — The sticky slate header (date nav + sport tabs) never actually sticks: .dmf-root uses min-height:100dvh so the page grows and the document scrolls, meaning .dmf-scroll (overflow-y:auto, :874) never overflows — .dmf-feedhead's position:sticky;top:0 (:876) is constrained to a scrollport that never scrolls.
  - *Disposition:* FIXED — document scroll restored (inner overflow removed), feedhead sticks at top:46px under the topbar
  - *Suggested fix:* Make the inner pane the real scroller: in DMF_CSS :846 change `min-height:100vh;min-height:100dvh` to `height:100vh;height:100dvh` (root is display:flex;flex-direction:column so .dmf-scroll{flex:1;overflow-y:auto} then owns scrolling and both sticky bars work as designed). Alternative if document scroll is preferred: remove overflow-y:auto from .dmf-scroll and set `.dmf-feedhead{top:47px}` (measured topbar height) so it stacks below the sticky topbar.
- **[P1 | tablet]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/GlobalMobileOwnerTabs.tsx:74` — 768px seam mismatch: the tab bar shows at innerWidth <= 768 but DimeModelFeed's bottom clearance applies only at max-width:767px — at exactly 768 (iPad Mini / 9.7" iPad portrait) the fixed 60px+safe-area tab bar permanently covers the last game card and the 1-800-GAMBLER responsible-gaming line.
  - *Disposition:* FIXED — isMobile is now strictly <768, agreeing with the CSS breakpoint
  - *Suggested fix:* Pick one seam and share it: change DimeModelFeed.tsx:875 to `@media (max-width:768px)` (minimum fix), and export a single MOBILE_TABS_MAX_WIDTH = 768 constant from features/mobileOwnerTabs/config.ts consumed by both the JS check (`window.innerWidth <= MOBILE_TABS_MAX_WIDTH`) and documented against the CSS. If the P0 fix extends tabs to <1024, move this media query to max-width:1023px in the same commit.
- **[P1 | tablet]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/GlobalMobileOwnerTabs.tsx:150` — The tab-bar "bottom spacer" is position:fixed, so it reserves zero layout space — it is a no-op — and BettingSplits (main pb-1, no feed-pb-safe) has its last game card occluded behind the tab bar whenever tabs are visible (≤768, incl. iPad Mini portrait).
  - *Disposition:* FIXED — replaced by body-level clearance
  - *Suggested fix:* Remove the `fixed bottom-0 left-0 right-0` classes from the spacer so it participates in flow (it renders after <Router/> inside #root, so a static div adds document height exactly where needed): `<div style={{height:"calc(60px + env(safe-area-inset-bottom,0px))"}} aria-hidden="true"/>`. Belt-and-braces: add `feed-pb-safe` to BettingSplits.tsx:644's main.
- **[P1 | tablet]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/dime-chat/DimeChatPage.tsx:483` — Chat is a first-class nav destination that dead-ends on tablets: the Chat tab (visible at ≤768) opens a 'built for desktop' refusal, and at 769–1023 the gate's only affordance links back to the feed — which itself has no nav (P0). The gate never tells iPad users that rotating to landscape (≥1024) works.
  - *Disposition:* PARTIAL — gate copy now tells tablet users landscape works; feed side has topbar nav; full tablet chat is follow-up
  - *Suggested fix:* Two-line fix inside ViewportGate: detect tablet (`matchMedia("(min-width:768px)")`) and render 'Rotate to landscape to open Dime chat' as the primary line, and add a second link to bettingSplitsPath("MLB") so the gate is never single-exit. Longer term, either drop the gate floor to 768px (the chat shell's 680px .dc-thread fits an 810px iPad portrait with the sidebar collapsed to a drawer) or remove/disable the Chat tab below 1024 with an explanatory tooltip instead of a dead tap.
- **[P2 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/GlobalMobileOwnerTabs.tsx:90` — shouldShow uses location.startsWith("/m") to exclude /m/* shell routes, but it also matches /mlb/team/:slug — owner tab bar silently disappears on MLB team schedule pages at ≤768 (incl. iPad Mini portrait).
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Change the check to `location === "/m" || location.startsWith("/m/")` (same fix pattern getActiveTab already uses in MobileOwnerBottomTabs.tsx:283).
- **[P2 | tablet]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:576` — Touch targets across the BettingSplits header are far below the 44pt floor on a touch-first tablet: sport pills ~30px tall, user-menu button 28×28, search-clear X ~12px, tab-bar buttons ~38px — none with hit-area extension.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add invisible hit-area extension to each control (relative + after:absolute after:-inset-2), or set min-h-[44px] on the pills/tab buttons and wrap the X in a p-2.5 button. E.g. pills: `className="... relative after:absolute after:-inset-2 after:content-['']"`.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:540` — The page tab bar nests a <button> inside wouter's <Link> (renders <a><button>…</button></a>) — invalid HTML, double tab stops, and ambiguous activation semantics for touch/AT.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Drop the inner <button> and style the Link directly: `<Link href={feedModelPath("MLB")} className="flex-1 flex items-center justify-center gap-2 py-2.5 …" aria-current={undefined}>…</Link>`, with `aria-current="page"` on the active Splits link instead of the button+underline-span structure.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:557` — The canonical Betting Splits surface still ships banned brand tokens: legacy neon #39FF14 active-tab underline, #22c55e search focus ring and scroll-pulse outline, gold Crown icons, red/amber badges — all explicitly proscribed by MASTER.md for Dime surfaces.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Swap the tab underline to mint: `background:"#45E0A8"`; search focus ring to `rgba(69,224,168,.35)`; scroll-pulse outline to `#45E0A8`/`rgba(69,224,168,.3)`. Track the full-page reskin (fonts, gold, reds) as the planned splits migration ticket.
- **[P3 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:859` — Global focus rule mutates element geometry — `:focus-visible{…border-radius:8px}` forces every focused control to 8px radius, visibly squaring the 18px-radius sport chips on keyboard/hardware-keyboard-tablet focus; the tablist also lacks arrow-key navigation.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Remove border-radius from the shared focus rule and let the ring follow each element's own radius: `.dmf-root :is(button,[tabindex]):focus-visible{outline:none;box-shadow:0 0 0 3px var(--dmf-ring)}` plus `border-radius:inherit` where needed; add ArrowLeft/ArrowRight handlers (or use plain buttons with aria-pressed instead of tablist semantics) on the sport chips.
- **[P3 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:292` — Two divergent theme systems: the feed keeps its own localStorage 'dime-feed-theme' toggle while chat follows ThemeContext — navigating feed→chat on an iPad landscape can flip dark→light with no user action.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Read/write the shared ThemeContext in DimeModelFeed (setTheme(next) → context), keeping the ?theme= query override for judge/pixel-verification links only.

### Lens: desktop

**Strengths:**
- DimeModelFeed's interaction primitives are reference-grade where they exist: 28px date arrows and sport chips extend to ~44px hit areas via ::after insets (DimeModelFeed.tsx:879,885 — Apple HIG applied on desktop too), with hover, :active press feedback, and the single brand curve 160ms cubic-bezier(.16,1,.3,1) throughout (878-887).
- Focus-visible is implemented as law on the feed: one scoped rule paints the 3px --dmf-ring on every button and tabindex element in both themes (DimeModelFeed.tsx:859), matching MASTER's focus spec exactly.
- Mint discipline is airtight in the feed surface: mint appears only on live dot, sig model values, edge footers, and the pick/edge verdict (896-938); PASS games dim to opacity .82 with grey verdicts and zero mint (891, 939) — precisely the 'negative = grey tiers + reduced opacity' rule; light mode swaps to #0FA36B tokens and adds the black keyline on mint fills (854, 866, 897).
- Accessibility media queries are first-class: prefers-reduced-motion kills all transitions and the live pulse (992-995), and prefers-contrast:more re-tiers borders and muted text for both themes (996-999) — few production dashboards ship the latter.
- Desktop long-slate ergonomics are half-right by design: sticky topbar (z-20) + sticky feedhead (z-10) keep the date nav and sport switcher available during a 15-game scroll with clean layering and a solid background (862, 876), per the brand's tiers+borders (no glass) law.
- Data typography meets the MASTER stat-block spec exactly: IBM Plex Mono 10px/0.08em uppercase micro-labels over Familjen Grotesk 700 values, tabular-nums on every numeral (860, 905-906, 924, 936) — no layout shift as odds tick.
- The mk3/mk7 container-query system (947-991) is a faithful port of the pixel-verified v4 reference including the span-2 last-column rule and the zero-truncation label-stacking floor, so the card grid degrades identically to the judged prototype.
- URL/navigation engineering under the hood is disciplined: all emitted paths go through feedRoutes.ts helpers, bare sport slugs canonicalize with history-replace so Back never lands on a dateless URL (DimeModelFeed.tsx:315-320), placeholderData/keepPreviousData prevent blank flashes, and getActiveTab is pathname-prefix-only so any dated slug keeps the correct tab lit (MobileOwnerBottomTabs.tsx:278-300).
- The chat side-nav was correctly repointed to canonical routes with aria-current on the active row and a sensible New-Chat reset interception (DimeChatPage.tsx:45-54, 180-198), and its profile menu handles Escape + outside-click per the MASTER menu spec.
- Responsible-gaming footer (21+ · 1-800-GAMBLER) is present on the canonical feed (DimeModelFeed.tsx:425), honoring the repo convention on a user-facing surface.

- **[P0 | desktop]** `client/src/pages/DimeModelFeed.tsx:357` — The canonical feed — the default post-login landing surface — is a desktop dead end: zero outbound navigation to chat, splits, or anywhere else.
  - *Disposition:* FIXED — same topbar nav
  - *Suggested fix:* Restore the 264px Dime sidebar shell from the v4 reference on ≥768px: a DimeShell component wrapping DimeModelFeed with navrows [New Chat → /chat, AI Model Projections (is-active), Betting Splits + Odds History → bettingSplitsPath('MLB'), Bet Tracker → /bet-tracker] using the MASTER.md .sidebar-row spec (9px 10px pad, radius 8, --row-hover, 3×18px mint active rail). Minimum viable stopgap if the shell is deferred: (1) wrap the wordmark in <Link href='/chat'>, (2) add two .dmf-chip-style links 'Chat' and 'Splits' to the .dmf-sync cluster (href={feedModelPath} pattern already exists in feedRoutes.ts). Either way the fix must ship links, not buttons with navigate(), so middle-click/cmd-click work on desktop.
- **[P1 | desktop]** `client/src/pages/DimeModelFeed.tsx:810` — Paging dates shows the PREVIOUS day's games under the NEW day's date label with no loading signal — a wrong-slate/wrong-date lie on a betting surface.
  - *Disposition:* FIXED — slate dims (opacity .45, 160ms) with aria-busy while placeholder data is on screen
  - *Suggested fix:* Surface isPlaceholderData/isFetching from useFeedCards (return isStale: isWc ? wcQuery.isPlaceholderData : mlbQuery.isPlaceholderData) and render <div className='dmf-list' aria-busy={isStale} data-stale={isStale}> with CSS .dmf-list[data-stale="true"]{opacity:.55;pointer-events:none;transition:opacity 160ms var(--dmf-ease)} — the existing brand curve. Also gate the slate count: show '…' (or keep the old count with the stale dim) while isStale.
- **[P1 | desktop]** `client/src/pages/BettingSplits.tsx:557` — The reconstruction-built feed↔splits tab bar ships the banned neon-green #39FF14 active indicator and legacy 'AI SPORTS BETTING' chrome, so the two canonical sibling surfaces present as two different products.
  - *Disposition:* FIXED — mint #45E0A8 underline; nested <button>-in-<Link> flattened; aria-current added
  - *Suggested fix:* In the tab bar only (537-560): replace #39FF14 with the mint token (#45E0A8 — signal-legal for an active-nav indicator per MASTER.md:22), swap the inline rgba(255,255,255,.45)/#fff colors for the grey text tiers, and render the identical two-tab bar on DimeModelFeed so the pair is symmetric — or supersede both with the shared sidebar shell from the P0 fix.
- **[P2 | desktop]** `client/src/pages/DimeModelFeed.tsx:874` — No max-width discipline: at 1920px the feed cards stretch to ~1840px, drifting Book/Model values 100px+ from their side labels and hollowing out the market columns.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Constrain the feed content: .dmf-scroll > *{max-width:1480px;margin-left:auto;margin-right:auto} (or an inner .dmf-container wrapper), tuned so mk3 market columns stay ≤360px. If the P0 sidebar shell lands, keep a ~1480px cap on the main pane anyway for 1920/2560 monitors. For BettingSplits add max-w-[1480px] mx-auto on <main> and the header rows 3-4.
- **[P2 | desktop]** `client/src/pages/DimeModelFeed.tsx:363` — The topbar 'sync cluster' contains no sync status — the page law's 'SYNCED N MIN AGO' + 6px mint dot is missing, so 60s-polled odds carry no visible freshness on a betting surface.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add to .dmf-sync before the theme button: <span className='dmf-ld' style={{width:6,height:6,animation:'none'}}/><span className='dmf-micro'>Synced {ago}</span>, deriving ago from mlbQuery.dataUpdatedAt/wcQuery.dataUpdatedAt with a 30s ticker (copy the splitsAgoLabel useMemo pattern from BettingSplits.tsx:384-395).
- **[P2 | desktop]** `client/src/pages/DimeModelFeed.tsx:874` — The slate scroll region is unreachable by keyboard: .dmf-scroll has no tabindex and game cards contain zero focusable elements, so keyboard-only users cannot scroll long slates.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add tabindex={0} role='region' aria-label='Projections slate' to the .dmf-scroll div (line 374). The existing focus rule .dmf-root :is(button,[tabindex]):focus-visible (859) already covers it — though give the region outline-offset:-3px so the ring isn't clipped by overflow.
- **[P2 | all]** `client/src/pages/BettingSplits.tsx:540` — The tab bar nests <button> elements inside wouter <Link> anchors — invalid HTML that creates double tab stops and confused screen-reader semantics on the primary cross-surface nav.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Drop the inner <button>: put the flex/typography classes on the Link itself (<Link href={…} className='flex-1 flex items-center justify-center gap-2 py-2.5 …'>). For the active tab render a non-link <div aria-current='page'> (or keep the Link with onClick={e=>e.preventDefault()}) so the active tab never pushes history.
- **[P2 | desktop]** `client/src/pages/BettingSplits.tsx:541` — No hover feedback on the page tabs or the MLB/NHL/NBA sport pills — primary desktop nav/filter controls give zero pointer response.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Move the static inline colors to classes and add hover states: inactive tab hover:text-white/75; pills — replace inline style pairs with a class pair (e.g. data-[active=true]:… ) plus hover:border-white/25 hover:text-white/70, all with transition-colors duration-150. Once the P1 rebrand of this bar lands, use --row-hover + the 160ms cubic-bezier(.16,1,.3,1) curve.
- **[P2 | desktop]** `client/src/pages/dime-chat/DimeChatPage.tsx:51` — Chat side-nav renders 'Trends' and 'Prop Projections' as normal-looking nav rows that silently do nothing on click (href='#' + preventDefault).
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Render the two inert rows in a visibly disabled state: aria-disabled='true', className+=' is-disabled' with .dc-sidebar-row.is-disabled{color:var(--text-faint);cursor:default;pointer-events:none} plus a mono micro-label 'SOON' chip on the right — keeps the frozen layout while removing the false affordance.
- **[P2 | all]** `client/src/pages/DimeModelFeed.tsx:870` — Theme toggle hover snaps instantly (no transition declared) and its Light/Dark label swap shifts the button width — both explicit MASTER anti-patterns on the only always-visible topbar control.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add to .dmf-themebtn: transition:color var(--dmf-t) var(--dmf-ease),border-color var(--dmf-t) var(--dmf-ease). Fix the width shift with min-width:56px;text-align:center (or a fixed-size sun/moon Lucide glyph, which MASTER's icon rule prefers over text). Optional polish: .dmf-root{transition:background var(--dmf-t) var(--dmf-ease),color var(--dmf-t) var(--dmf-ease)} to ease the theme flip, auto-disabled by the existing reduced-motion block (992-995).
- **[P2 | desktop]** `client/src/pages/DimeModelFeed.tsx:415` — Empty dates dead-end with 'Try the date arrows above' instead of the page contract's auto-advance-to-first-available-date; arrows page blindly through unbounded empty days.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Keep the URL stable but make the empty state actionable: query the sport's available dates (games.list already returns the rolling window; or reuse getCurrentDate + a lightweight dates probe) and render a .dmf-chip link 'Jump to next slate → {prettyDate(nextDate)}' via feedModelPath(sport, nextDate). Also disable/dim the arrow that walks further away from the nearest slate when that data is available.
- **[P3 | desktop]** `client/src/pages/DimeModelFeed.tsx:388` — Sport switcher uses role='tablist'/'tab' without the ARIA tabs keyboard contract (no roving tabindex, no arrow-key handling, no aria-controls).
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Either drop the tab roles (these are navigation links — a simple <nav aria-label='Sport'> with aria-current='true' on the active chip is honest and keyboard-correct), or implement roving tabindex + ArrowLeft/ArrowRight. Given they navigate URLs, prefer the nav-link semantics.
- **[P3 | desktop]** `client/src/pages/DimeModelFeed.tsx:326` — Clicking the already-active sport chip pushes a duplicate history entry, degrading the Back button.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Guard in go(): const next = feedModelPath(nextSport, nextIso); if (next !== window.location.pathname) navigate(next); — or pass replace:true when only the sport chip re-asserts the current URL.
- **[P3 | all]** `client/src/pages/DimeModelFeed.tsx:859` — The shared focus-visible rule hard-codes border-radius:8px, so the 18px-radius sport chips visibly change shape when keyboard-focused.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Remove border-radius from the shared rule and let each control keep its own radius (box-shadow rings follow border-radius automatically); the 8px default belongs only on radius-less controls, e.g. .dmf-themebtn already sets its own.
- **[P3 | desktop]** `client/src/pages/DimeModelFeed.tsx:292` — Theme state is fragmented across the three surfaces (feed: localStorage 'dime-feed-theme'; chat: ThemeContext; splits: hard dark) — crossing surfaces can flash dark↔light abruptly.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Back the feed toggle with the same ThemeContext the chat uses (write-through to the shared store, keep 'dime-feed-theme' only as a migration read), so one preference follows the user across Dime surfaces.
- **[P3 | desktop]** `client/src/pages/DimeModelFeed.tsx:232` — Loading skeletons only populate the 250px matchup column — at 1280-1920 the card's market/verdict area renders as a large blank slab and the settled card is much taller (minor CLS).
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add three .dmf-skel column blocks in a .dmf-markets placeholder and one in .dmf-verdict, sized to match a settled mk3 card's height (~170px), so the skeleton reserves true layout.
- **[P3 | desktop]** `client/src/pages/DimeModelFeed.tsx:377` — Date-nav arrows are typographic ‹ › glyphs, not brand-kit/Lucide SVGs, with baseline-dependent centering; no 'Today' return affordance after paging away.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Swap in Lucide ChevronLeft/ChevronRight at 16px stroke 2 inside the existing .dmf-sq buttons; make the date label a button that returns to feedModelPath(sport) (today), with title='Back to today' and the standard 160ms hover.
- **[P3 | desktop]** `client/src/pages/DimeModelFeed.tsx:835` — Missing the reference's quiet scrollbar styling — default chunky scrollbars sit against the dark slate at desktop.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Port the two reference rules into DMF_CSS scoped to .dmf-scroll (plus scrollbar-width:thin;scrollbar-color:var(--dmf-border-hi) transparent for Firefox).
- **[P3 | all]** `client/src/pages/DimeModelFeed.tsx:338` — The 'Invalid feed URL' state is itself a dead end — explains the slug format but offers no link to today's slate, and forces dark theme.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add <a href={feedModelPath('MLB')} className='dmf-chip dmf-active'>Open today's MLB slate</a> under the message and pass the resolved theme state instead of the literal 'dark'.
- **[P3 | all]** `client/src/pages/DimeModelFeed.tsx:839` — Muted text tiers used for data labels sit below MASTER's own 4.5:1 checklist line — worst in light mode (#9A9AA8 on white ≈ 2.8:1).
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Not fixable in this file alone — escalate to the brand owner: either amend MASTER's checklist to AA-large for ≤11px mono micro-labels (documenting the exception) or nudge tokens (dark t4 → #7E7E8C, light t4 → #808089, mint-on-light → #0B8A5C) and re-verify against the reference pages. Flagging per intended-vs-implemented; do not silently drift tokens.
- **[P3 | desktop]** `client/src/pages/BettingSplits.tsx:431` — Search jump-to-game pulse uses legacy green #22c55e with a heavy 24px glow and 300ms JS interval flashing — off-brand accent and loud motion; plus dead filter state and unused imports left in the touched file.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* When this page is rebranded: replace the pulse with a single 160ms box-shadow ring in var(--ring) that fades after 1.2s (guarded by prefers-reduced-motion). Now: delete headerHeight/ResizeObserver, liveCount, toggleStatus and the unused imports.

### Lens: motion-interaction

**Strengths:**
- DimeModelFeed centralizes motion tokens (`--dmf-t:160ms`, `--dmf-ease:cubic-bezier(.16,1,.3,1)`, DimeModelFeed.tsx:841) and every declared transition on the surface consumes them (.dmf-sq line 878, .dmf-chip line 884) — the one-curve law is genuinely enforced, not decorative.
- DimeModelFeed's reduced-motion block (lines 992-995) kills all transitions AND the live-dot pulse (holding opacity:1, exactly MASTER's rule), and it goes beyond the checklist with a prefers-contrast:more token override (lines 996-999).
- Live indicator matches MASTER.md to the pixel and millisecond: 7px mint dot, 1.6s opacity pulse (lines 896-898), with the light-theme hairline keyline (line 897) per the brand coin-dot rule.
- Touch ergonomics on the feed head are done right: 28px date arrows, theme button, and sport chips all extend their hit area via ::after inset:-8px/-6px pseudo-elements (lines 871, 879, 885) — 44px effective targets with zero layout cost — and .dmf-sq:active applies background instantly (line 881) because background is deliberately excluded from the transition list: true pointer-down feedback.
- placeholderData/keepPreviousData on both feed queries (lines 810, 820) keeps the previous slate mounted while paging dates — no skeleton flash, no scroll reset, fully interruptible SPA navigation (it only needs the stale-state signal flagged above).
- Crest/flag images occupy a fixed box (width/height/flex-basis set before load, Crest component lines 101-129 + .dmf-crest line 903) with an onError monogram fallback — zero layout shift from logo loading, no broken-image states.
- Dime-chat sidebar rows (nav-touched) are the reference implementation of MASTER's component spec: 160ms brand curve on background/color/transform, an active mint rail that animates opacity + scaleY(0.7→1) (frozen-tokens.css:71-78), and a dedicated reduced-motion override (line 81).
- The DimeChat viewport gate is fully static (reduced-motion safe by construction, conversation.css:187-189) and — because all hooks keep running below the 1024px floor (DimeChatPage.tsx:793-798) — an active SSE stream survives a temporary resize: interruptibility applied at the architecture level, not just the animation level.
- DimeChatPage gates its smooth scrolling behind prefersReducedMotion() (lines 77-79, 789) and uses a matchMedia change listener for the viewport gate instead of a resize loop (lines 562-569) — the exact patterns the other audited surfaces should copy.
- MobileOwnerBottomTabs meets the 44px touch-target floor with safe-area-inset padding (lines 176, 215-216), navigates SPA-only with no window.location fallback (lines 146-149), and getActiveTab's prefix matching keeps the correct tab lit across every dated/sport URL variant (lines 278-300) — wayfinding stays coherent through the new canonical routes.

- **[P1 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:810` — Date paging shows the previous day's slate under the new date header with zero stale-data signal — placeholderData keeps old cards mounted, and nothing consumes isFetching/isPlaceholderData
  - *Disposition:* FIXED — same stale-slate dimming
  - *Suggested fix:* Return the placeholder flag from useFeedCards: `const isStale = isWc ? wcQuery.isPlaceholderData : mlbQuery.isPlaceholderData; return { cards, isLoading, isStale, gamesCount };` then render `<div className={\`dmf-list${isStale ? " dmf-stale" : ""}\`}>` and add to DMF_CSS: `.dmf-list{transition:opacity var(--dmf-t) var(--dmf-ease)} .dmf-list.dmf-stale{opacity:.45;pointer-events:none}`. Optionally swap the count for an en-dash while stale: `{isStale ? "–" : gamesCount}`.
- **[P1 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/GlobalMobileOwnerTabs.tsx:150` — The 'bottom spacer' is position:fixed, so it reserves no layout space — and BettingSplits has no clearance class, so the last game card is permanently occluded behind the opaque 60px tab bar
  - *Disposition:* FIXED — body.mobile-owner-tabs-active reserves real flow space (fixed no-op spacer removed)
  - *Suggested fix:* Delete the fixed spacer in GlobalMobileOwnerTabs.tsx:149-154 (it does nothing), and add the clearance class in BettingSplits.tsx:644: `className="w-full pb-1 feed-pb-safe"`. Better long-term: make the rule global — `body.mobile-owner-tabs-active { padding-bottom: calc(60px + env(safe-area-inset-bottom,0px)); }` — so every future canonical surface clears the bar without opting in.
- **[P1 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:222` — Bottom tab buttons give zero press-down feedback: the native tap highlight is suppressed (WebkitTapHighlightColor: transparent) and nothing replaces it
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Give the button a class and move press feedback to CSS (inline styles cannot express :active): `.mot-tab:active { transform: scale(0.94); }` with `.mot-tab { transition: transform 160ms cubic-bezier(0.16,1,0.3,1); }` and `@media (prefers-reduced-motion: reduce){ .mot-tab { transition: none } .mot-tab:active { transform: none; opacity: .7 } }`. The spec-locked colors stay inline; press scale doesn't touch them.
- **[P1 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:429` — scrollToGame runs a smooth scroll plus a 1.5s flashing outline animation with no prefers-reduced-motion check, and its setInterval/setTimeout are never cleaned up on unmount
  - *Disposition:* FIXED — honors prefers-reduced-motion (auto scroll, static highlight); pulse recolored to mint
  - *Suggested fix:* Gate motion: `const reduce = window.matchMedia?.("(prefers-reduced-motion: reduce)").matches; el.scrollIntoView({ behavior: reduce ? "auto" : "smooth", block: "center" });` — under reduce, apply one static outline for ~1.2s instead of the interval pulse. Store timer ids in a ref (`timersRef.current.push(id)`) and clear them in a useEffect cleanup. Restyle: `outline: 2px solid rgba(69,224,168,.9); box-shadow: 0 0 0 4px rgba(69,224,168,.28); transition: box-shadow 160ms cubic-bezier(0.16,1,0.3,1), outline-color 160ms cubic-bezier(0.16,1,0.3,1)`.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:366` — Theme toggle hard-cuts the entire surface dark↔light — no easing on the token swap
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add to DMF_CSS: `.dmf-root, .dmf-topbar, .dmf-feedhead, .dmf-game, .dmf-mkbox, .dmf-mkfoot, .dmf-verdict { transition: background-color var(--dmf-t) var(--dmf-ease), color var(--dmf-t) var(--dmf-ease), border-color var(--dmf-t) var(--dmf-ease); }`. The existing reduced-motion block (line 993 `.dmf-root *{transition:none !important}`) already neutralizes it for reduce users.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:645` — Initial load and every sport-pill switch collapse the whole page to a centered spinner — blocking loading state with full-page layout shift, instead of skeletons
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Replace the spinner block with 4-6 skeleton cards whose min-height matches GameCard's rendered height (e.g. `<div className="animate-pulse bg-card border-b border-border" style={{ minHeight: 148 }} />`), so the list geometry is reserved and the transition is a fade, not a collapse. Keep the spinner only as a small inline indicator in the filter bar if desired.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:232` — Skeleton→card geometry mismatch: SkeletonRow is always the short mk3 matchup-only shape, so first paint shifts ~30px per card on MLB and ~300px per card on World Cup
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Parameterize the skeleton: `function SkeletonRow({ wc }: { wc?: boolean })` — for MLB render three ghost market boxes in the `dmf-markets` grid (a `dmf-skel` block of height ~100px per column) plus a verdict-column ghost; for WC use `dmf-mk7` with 7 ghost boxes. Call `<SkeletonRow wc={sport === "WC"} />` and render 5-6 of them.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:576` — Page-wide off-brand motion tokens: Tailwind transition-colors/transition-all (150ms, cubic-bezier(0.4,0,0.2,1)) everywhere instead of the Dime 160ms cubic-bezier(0.16,1,0.3,1) single curve
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Define once in index.css: `.brand-t { transition-property: color, background-color, border-color, box-shadow; transition-duration: 160ms; transition-timing-function: cubic-bezier(0.16,1,0.3,1); }` (plus a reduced-motion override), then replace every `transition-colors`/`transition-all duration-150` on this page with `brand-t`. Never use `transition-all` on the pills.
- **[P2 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:134` — navigator.vibrate(10) fires on every tap — including re-taps of the already-active tab where nothing happens on screen
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Move the `navigator.vibrate(10)` call inside the `if (location !== path) { ... }` block at line 146, so the haptic accompanies an actual navigation.
- **[P2 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:223` — Tab bar animates with 150ms ease-out (three declarations) instead of the brand 160ms cubic-bezier(0.16,1,0.3,1), and — being inline styles — has no prefers-reduced-motion path at all
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Move the three transitions into a stylesheet class (e.g. `.mot-tab-icon { transition: transform 160ms cubic-bezier(0.16,1,0.3,1), color 160ms cubic-bezier(0.16,1,0.3,1); }`) and add `@media (prefers-reduced-motion: reduce) { .mot-tab-icon { transition: none; transform: none !important; } }`. The color tokens can stay inline per the file's spec header — only the motion needs to live in CSS.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:540` — Page-tab bar nests <button> inside wouter <Link> (renders <a><button>…) — invalid interactive nesting that produces double focus stops and unreliable press behavior
  - *Disposition:* FIXED — links style themselves; aria-current="page" on the active tab
  - *Suggested fix:* Style the Link directly and delete the inner button: `<Link href={feedModelPath("MLB")} className="flex-1 flex items-center justify-center gap-2 py-2.5 text-sm font-bold tracking-wide brand-t" style={{ color: "rgba(255,255,255,0.45)" }}>…</Link>`. Same for the self-referencing BETTING SPLITS tab (or render the active tab as a non-link `<span aria-current="page">`, which also removes the pointless self-navigation).
- **[P2 | desktop]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:870` — Theme button hover state changes color/border-color with no transition — an instant state change, against MASTER's 'hover states use the 160ms brand curve' checklist
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Append to line 870: `transition:color var(--dmf-t) var(--dmf-ease),border-color var(--dmf-t) var(--dmf-ease)`.
- **[P3 | desktop]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/dime-chat/DimeChatPage.tsx:51` — 'Trends' and 'Prop Projections' nav rows are dead (href="#" + preventDefault) but look and feel identical to live links — click produces no feedback whatsoever
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Render dead rows as visibly inert: `<span key={row.label} className="dc-sidebar-row is-disabled" aria-disabled="true"><span className="dc-sidebar-text">{row.label}</span><span className="dc-microlabel" style={{marginLeft:"auto",opacity:.6}}>SOON</span></span>` with `.dc-sidebar-row.is-disabled{opacity:.5;cursor:default} .dc-sidebar-row.is-disabled:hover{background:transparent;color:var(--text-secondary)}`.
- **[P3 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:944` — Skeleton blocks are fully static — no shimmer/pulse, so a slow first load is indistinguishable from a frozen page
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add `.dmf-skel{animation:dmf-skelpulse 1.6s var(--dmf-ease) infinite}` reusing `@keyframes dmf-pulse`-style opacity (`0%,100%{opacity:.55}50%{opacity:1}`), and extend the reduced-motion block (line 992) with `.dmf-skel{animation:none}`.
- **[P3 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:463` — User menu and search dropdown pop in with zero enter transition and no origin anchoring
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add a shared class: `.menu-enter{transform-origin:top right;animation:menuIn 160ms cubic-bezier(0.16,1,0.3,1)} @keyframes menuIn{from{opacity:0;transform:scale(.98)}to{opacity:1;transform:scale(1)}} @media (prefers-reduced-motion:reduce){.menu-enter{animation:none}}` and apply it to both popovers (search dropdown origin: top left).
- **[P3 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:426` — Artificial 120ms setTimeout on the search-result tap path before scrolling begins
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Replace the timer with a double requestAnimationFrame after the state updates flush: `requestAnimationFrame(() => requestAnimationFrame(() => { const el = document.getElementById(...); ... }))` — scrolling starts on the first frame after React commits the dropdown close.
- **[P3 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:198` — headerHeight state is written by a ResizeObserver but never read — every header resize triggers a dead full-page re-render
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Delete the headerHeight state and the ResizeObserver effect (lines 197-206), or actually consume the value — e.g. set `scroll-margin-top: headerHeight` on game-card wrappers so scrollToGame clears the sticky header deterministically instead of relying on block:'center'.
- **[P3 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:254` — Active indicator dot is conditionally mounted, so it pops in/out with no transition when the tab changes
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Render the dot unconditionally and animate opacity/scale: `style={{ …, opacity: isActive ? 1 : 0, transform: isActive ? "scale(1)" : "scale(0.5)" }}` with the same 160ms brand-curve transition class as the icon (and the same reduced-motion override).
- **[P3 | tablet]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/GlobalMobileOwnerTabs.tsx:74` — Mobile detection uses a raw resize listener with an inclusive <=768 boundary that disagrees with DimeModelFeed's max-width:767px clearance media query
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Replace the resize listener with `const mq = window.matchMedia("(max-width: 767px)"); setIsMobile(mq.matches); mq.addEventListener("change", e => setIsMobile(e.matches));` — one boundary (≤767) shared with the DMF media query, change-events only.
- **[P3 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/dime-chat/frozen-tokens.css:62` — Viewport-gate link (.dc-link) hover color snaps with no transition — the one nav-touched Dime-chat element outside the 160ms law
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Add `transition: color 160ms cubic-bezier(0.16,1,0.3,1);` to `.dc-link` (line 62) and include `.dc-link` in the reduced-motion override at line 81, pending frozen-spec owner approval.

### Lens: brand-a11y

**Strengths:**
- DimeModelFeed dark theme is a faithful MASTER.md transcription: every surface/border/text token verbatim (DimeModelFeed.tsx:836-856), and dark-mode contrast passes AA with margin — values #C9C9D4 10.97:1, micro-labels t3 6.2-7.1:1, mint signal 10.27:1 on card-hi, edge footer 8.39:1 on the mint-dim tint (only the t4 tier fails, per findings).
- One-accent mint discipline is genuinely enforced on the feed: mint appears ONLY on .dmf-sig model values, edge footers, LIVE dot/label, the coin-dot, and focus rings; PASS/no-edge is grey + 82% row fade exactly per MASTER line 48 (DimeModelFeed.tsx:891, 926, 929, 938); zero gradients, zero red/gold/purple in DMF_CSS.
- Both accessibility media features are handled in DMF_CSS — prefers-reduced-motion kills all transitions and the live-dot pulse (992-995), and prefers-contrast:more ships darker borders and lifted muted tiers for BOTH themes (996-999) — rarer than it should be in production code.
- Motion law compliance: a single 160ms cubic-bezier(.16,1,.3,1) custom-property curve used everywhere (841), 1.6s live-dot pulse at 7px per spec (896-898), no scroll/parallax effects anywhere on the data surfaces.
- Brand coin-dot rule implemented correctly per theme: mint dot on dark, mint dot + #0B0B0F hairline keyline on light for both the wordmark dot and the live dot (866, 897) — matches MASTER line 49 exactly.
- Icon-only controls on the feed all carry aria-labels (theme toggle with state-aware label at 367, Previous/Next day at 377/381), and small controls extend hit areas with ::after inset padding (871, 879, 885) — the 28px date arrows reach ~44px effective targets.
- Crest/flag images are properly decorative: wrapper aria-hidden + alt="" + lazy loading + graceful monogram fallback on error with 2-char clipping guard (Crest, 101-130).
- Zero-truncation responsive strategy via container queries: markets reflow 7→4→2 columns, labels stack above values under 440px, win% annotations drop rather than clip (961-991) — no horizontal scroll path down to 360px, honoring the page contract.
- Bottom tabs get the mechanics right even where the palette is wrong: 44px minimum targets, env(safe-area-inset-bottom), icon+label (never icon-only), per-tab aria-label, inactive text at 6.25:1 on black, and pathname-only getActiveTab keeps dated feed URLs lit correctly (MobileOwnerBottomTabs.tsx:216, 176, 281-283).
- Navigation contract hygiene in the audited surfaces is clean: every emitted link is canonical (NAV_ROWS DimeChatPage.tsx:49-50, ViewportGate:489, tab config config.ts:30-31, BettingSplits self-link 551 fixing the old tab-unselects-itself bug) — no /feed, /splits, or ?tab= slug survives in any audited link target.
- Responsible-gaming law respected on both canonical surfaces: '21+ · Gambling problem? 1-800-GAMBLER' on the feed (DimeModelFeed.tsx:425) and in the chat disclaimer (DimeChatPage.tsx:73).
- BettingSplits carries some real a11y care despite its legacy skin: TeamBadge enforces a 32px minimum (97), abbreviation-based no-truncation responsive names in search results (148-167), and ResizeObserver-driven header height instead of magic numbers (200-206).

- **[P1 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:42` — Bottom nav active icon and indicator dot are neon green #39FF14 — the explicitly banned legacy accent (MASTER.md line 23 and Anti-Patterns line 214: 'Neon green #39FF14 — legacy accent, replaced by mint everywhere').
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Change ACTIVE_ICON and DOT to "#45E0A8" (mint on dark stays raw mint — 10+:1 on black). Rewrite the header comment to cite design-system/dime-ai/MASTER.md as the token source so the next agent doesn't 'restore' neon.
- **[P1 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:557` — Active-tab underline on the canonical Betting Splits page is banned neon green: style={{ background: "#39FF14" }}.
  - *Disposition:* FIXED — mint #45E0A8
  - *Suggested fix:* background: "#45E0A8". While there, the active tab should also gain aria-current="page" (see the Link/button nesting finding) so active state isn't conveyed by color alone.
- **[P1 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:577` — Sport pills use three off-brand team-color accents, two of which fail AA: MLB active #E31837 on rgba(0,45,114,.25)-over-bg = 3.69:1 and NBA active #C8102E on rgba(200,16,46,.15)-over-bg = 2.93:1 at 14px/700 text (needs 4.5:1). NHL #4FC3F7 passes (8.04:1) but is still a second accent.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Active pill: background var(--surface-raised)/#1A1A22-equivalent, color text-primary, optional 3px mint left rail or inset mint keyline; inactive: --text-muted per the MASTER Buttons/pills spec (.pill active = surface-raised bg). Keep the league logo img as the only league identification. Raise inactive text to rgba(255,255,255,0.55)+.
- **[P1 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:454` — Brand-row icon (BarChart3 className="text-primary", line 454) and the page loading spinner (Loader2 className="text-primary", line 647) render in PURPLE — index.css:125 defines --primary as oklch(0.55 0.18 275) with the comment 'Purple accent — matches the reference site's #6C63FF'. Purple is a named MASTER.md anti-pattern.
  - *Disposition:* FIXED — brand icon + spinner forced to mint #45E0A8 (page-level; global --primary token untouched)
  - *Suggested fix:* Point these two elements at mint explicitly (className="text-[#45E0A8]" or a brand token class), and file the real fix upstream: retire the purple --primary/--ring in client/src/index.css in favor of the Dime tokens (mint accent, rgba(69,224,168,.36) ring).
- **[P1 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:853` — Light theme --dmf-t4:#9A9AA8 fails AA hard everywhere it labels data: 2.78:1 on #FFFFFF, 2.60:1 on #F7F7F9 card, 2.53:1 on #F4F4F6 — used at 10px for market column headers (Side/Book/Model, line 917), game meta (line 907), verdict labels Pick/Edge/Grade (line 935), and the 'NO EDGE' status footer (line 930). PASS rows compound with .dmf-pass opacity:.82 (line 891) down to 2.12:1.
  - *Disposition:* FIXED — light t4 → #6A6A78 (≈4.9:1 on card)
  - *Suggested fix:* In .dmf-root[data-dmf-theme="light"] set --dmf-t4:#6E6E7A (5.03:1 on white, 4.58:1 on #F4F4F6). Exempt labels from the pass-row fade: .dmf-pass .dmf-vl,.dmf-pass .dmf-mkhead span{opacity:1} or scope the .82 to values only. Record the errata in design-system/dime-ai/MASTER.md.
- **[P1 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:854` — Light theme mint signal text fails AA: --dmf-mint:#0FA36B gives 2.95:1 for 15px/700 .dmf-sig model values on the #F4F4F6 mkbox (line 926) and 2.73:1 for the 11px/500 edge footer on the mint-dim tint (#0FA36B on rgba(15,163,107,.08) over #F4F4F6 = blended #E2EEEB, line 929).
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* In the light theme block set --dmf-mint:#0B8154 (4.90:1 white, 4.46:1 on #F4F4F6) or #0A7A50 (5.37/4.89:1) for text usage; keep #45E0A8 only for fills with the black hairline keyline per MASTER line 47. Propose updating MASTER.md --mint-on-light to the darker value.
- **[P1 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/dime-chat/DimeChatPage.tsx:489` — ViewportGate's only actionable element — the Link to /feed/model/mlb — uses .dc-link raw mint #45E0A8 with no light-theme override (frozen-tokens.css:62): 1.68:1 on the white light-theme page (hover #7DEBC4 is 1.44:1). Direct MASTER.md anti-pattern line 218: 'Raw mint #45E0A8 as TEXT on light surfaces'.
  - *Disposition:* FIXED — .theme-light gate link renders #0FA36B
  - *Suggested fix:* Add .theme-light .dc-link{color:var(--mint-on-light)} (and a hover tier) in frozen-tokens.css — the variable already exists at :root (line 50). Give the gate link display:inline-block;padding:12px to clear 44px touch height.
- **[P1 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:540` — Page tab bar nests <button> inside wouter <Link> (an <a>) at lines 540-547 and 551-559 — invalid interactive-inside-interactive HTML; keyboard users hit two tab stops per tab, screen readers announce link and button inconsistently; and the active tab has no aria-current, so active state is conveyed by color/underline only.
  - *Disposition:* FIXED — links style themselves; aria-current="page" on the active tab
  - *Suggested fix:* Render the Link as the styled element: <Link href={...} className="flex-1 flex items-center justify-center …" aria-current={active ? "page" : undefined}> and delete the inner <button>. Wrap the pair in <nav aria-label="Feed pages">.
- **[P1 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:644` — Last game card is permanently hidden behind the fixed 60px bottom nav for owners on mobile: <main className="w-full pb-1"> has no clearance, the .mobile-owner-tabs-active .feed-pb-safe rule (index.css:222) only matches ModelProjections.tsx:1559, and GlobalMobileOwnerTabs' 'spacer' (GlobalMobileOwnerTabs.tsx:150-154) is position:fixed + pointer-events-none, which reserves zero layout space.
  - *Disposition:* FIXED — same clearance fix
  - *Suggested fix:* Add feed-pb-safe to the BettingSplits <main> (className="w-full pb-1 feed-pb-safe"), or fix it globally: delete the fixed spacer and have GlobalMobileOwnerTabs set document.body.style.paddingBottom = 'calc(60px + env(safe-area-inset-bottom,0px))' while shouldShow (it already toggles a body class — extend index.css: body.mobile-owner-tabs-active{padding-bottom:calc(60px + env(safe-area-inset-bottom,0px))}).
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:839` — Dark theme --dmf-t4:#6A6A78 is below AA for its 10px text uses: 3.38:1 on #16161C (game meta, line 907) and 3.25:1 on #1A1A22 (mkhead Side/Book/Model line 917, 'NO EDGE' line 930, verdict labels line 935); PASS-row fade compounds to 2.63:1.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Set dark --dmf-t4:#82828F (4.75:1 on card, 4.56:1 on card-hi) — visually still clearly a 'faint' tier under t3. Exempt labels from the .dmf-pass 82% fade as in the light-theme finding.
- **[P2 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:178` — ARIA tabs misuse on the bottom nav: <nav role="tablist"> (line 178-179) destroys the navigation landmark, role="tab" buttons (line 201) have no aria-controls/tabpanel, and the required tablist keyboard pattern (arrow-key roving tabindex) is not implemented — yet the controls actually perform URL navigation, which is link semantics, not tab semantics.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Bottom nav: drop role="tablist"/"tab"; render <nav aria-label="Main"> containing <a href> elements (wouter Link) with aria-current="page" on the active item — keep the styling. DimeModelFeed chips: same conversion (they change the URL), or if tab semantics are kept, add roving tabindex + ArrowLeft/ArrowRight handlers and aria-controls pointing at the list container.
- **[P2 | mobile]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/MobileOwnerBottomTabs.tsx:299` — getActiveTab falls back to "feed" for any unmatched path (line 299), so on /bet-tracker, /admin/*, /login etc. the Feed tab renders lit-active with aria-selected="true" while the user is not on the feed — a lying nav state.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* return null; in the fallback and let no tab be active (the map already handles activeTabId === tab.id being false for all). Keep the prefix matches for the canonical surfaces (lines 281-283) as-is — those are correct.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:15` — Raster CDN PNG icons with filter:invert(1) (CDN_TEST_TUBE/CDN_MONEY_BAG/CDN_NBA, lines 15-17, used at 544, 555, 578-style pills, 652) violate the brand SVG-icon law, and their alt texts are screen-reader noise: 'Test tube AI MODEL PROJECTIONS', 'Money bag BETTING SPLITS'; the empty-state test tube (line 652) has a meaningless alt.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Replace with Lucide (FlaskConical and a money/DollarSign glyph are already imported in the codebase) or the brand SVG kit (dime-ai/design-bundle/dime-brand-svgs/). All icons that sit next to visible labels get alt="" (or aria-hidden="true"); the empty-state icon gets alt="" since 'No games found' text follows.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:471` — User-menu and status accents break one-accent law with four extra hues: gold Crown text-yellow-400 (lines 471, 501 — MASTER bans gold outright), text-green-400 (498), text-red-400 (504) and red/amber session badges (485-486 — MASTER: negative states are grey, never red), text-emerald-400 (528), plus non-brand Tailwind green #22c55e / rgba(34,197,94,…) on the search focus ring (599) and scroll-to-game pulse (431-437).
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Positive/active signal → #45E0A8; warnings/negative → grey tiers with reduced opacity per MASTER line 48 (or keep red-400 strictly for destructive Security Events if product requires, documented as an exception); focus ring → rgba(69,224,168,0.36); Crown → neutral outline icon, mint fill when active (MASTER anti-pattern line 215 pattern).
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:424` — scrollToGame highlight pulse (lines 424-441) hardcodes #22c55e outline/glow, loops a 300ms on/off flash 5 times via setInterval, and ignores prefers-reduced-motion entirely (smooth scrollIntoView + flashing).
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* const reduce = matchMedia('(prefers-reduced-motion: reduce)').matches; use behavior: reduce ? 'auto' : 'smooth'; replace the interval flash with a single static ring for ~1.6s: box-shadow 0 0 0 3px rgba(69,224,168,.36) with the 160ms ease, then remove. Clear the interval in a cleanup ref.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:601` — Search input is placeholder-only ('Search…', line 601) with no accessible name, no combobox/expanded semantics for the results dropdown, and the user menu (line 460) opens with no aria-haspopup/aria-expanded, no Escape-to-close, and no focus management.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Input: aria-label="Search games" role="combobox" aria-expanded={showDropdown}; results container role="listbox" (rows role="option") or at minimum an aria-live count. Menu button: aria-label={user ? "Account menu" : "Sign in"} aria-haspopup="menu" aria-expanded={showUserMenu}; add a keydown Escape handler that closes and returns focus to the button.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:859` — Focus indicator robustness: the 35%-alpha mint ring computes to 2.32:1 against the dark page and just 1.48:1 on light (rgba(15,163,107,.35) blended = #ABDECB on white) — under the 3:1 focus-indicator guidance — and because the rule sets outline:none, Windows forced-colors mode strips the box-shadow leaving NO focus indicator at all.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Use outline:3px solid transparent; outline-offset:0 alongside the box-shadow (transparent outline becomes visible in forced-colors); raise light ring to rgba(15,163,107,.55) or solid #0FA36B; replace border-radius:8px with border-radius:inherit.
- **[P2 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/BettingSplits.tsx:455` — The entire canonical Betting Splits surface renders in the banned legacy font: body font-family 'Inter' (index.css:173) — MASTER.md anti-pattern line 220 ('Barlow Condensed / Inter / JetBrains Mono — legacy fonts') — with zero Familjen Grotesk or IBM Plex Mono anywhere on the page, and the header still reads 'AI SPORTS BETTING' (line 455) rather than the Dime identity.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Minimum viable brand pass: set the page (or body) font-family to "Familjen Grotesk", with IBM Plex Mono for the tracking-widest micro-labels (date header line 629-636); swap the header row for the dime wordmark + 'Betting Splits' title per MASTER page-shell spec. Full reskin can follow the DIME-FEED-MIGRATION pattern.
- **[P3 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/dime-chat/DimeChatPage.tsx:51` — Dead sidebar nav rows 'Trends' and 'Prop Projections' (NAV_ROWS lines 51-52) render as focusable <a href="#"> whose click is preventDefault'd (lines 176-178) — keyboard/SR users tab to them, activate, and nothing happens, with no aria-disabled and no visual disabled treatment.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Render the href="#" rows as <a role="link" aria-disabled="true" tabIndex={-1} className="dc-sidebar-row is-disabled"> with a muted opacity tier, or as plain <span className="dc-sidebar-row"> — the frozen visual is preserved either way.
- **[P3 | tablet]** `/home/user/ai-sports-betting-dime-ai/client/src/features/mobileOwnerTabs/GlobalMobileOwnerTabs.tsx:74` — Breakpoint off-by-one: tabs mount at window.innerWidth <= 768 (line 74) but DimeModelFeed's clearance query is @container/media max-width:767px (DimeModelFeed.tsx:875) — at exactly 768px CSS width (iPad Mini portrait) the tabs render while .dmf-scroll keeps only its default 60px bottom padding, exactly the nav height with zero slack or safe-area allowance.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Change the JS check to window.matchMedia('(max-width: 767px)').matches (also replacing the resize listener with a matchMedia change listener, as DimeChatPage already does at lines 563-569), so JS and CSS share one boundary.
- **[P3 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:865` — Wordmark coin-dot sits at top:.02em (line 865) vs the MASTER.md wordmark law top:0.05em (MASTER line 56); and the aria-label="dime" lives on a plain <span> (line 358) where accessible-name computation is unreliable without a role.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* top:.05em; add role="img" to the wordmark span (role img + aria-label="dime" is the reliable pattern for a styled-text logo).
- **[P3 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:409` — Loading state is silent for screen readers: SkeletonRows are aria-hidden (line 234) and nothing announces loading or the later arrival of N games; the empty state (line 416) is also un-announced on date changes.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Wrap the slate count span (line 384) in aria-live="polite", and add a visually-hidden <span role="status">Loading games…</span> beside the skeletons while isLoading.
- **[P3 | all]** `/home/user/ai-sports-betting-dime-ai/client/src/pages/DimeModelFeed.tsx:292` — Feed theme is page-local state (localStorage 'dime-feed-theme' + ?theme= param, lines 292-308) while the chat surface reads ThemeContext (DimeChatPage.tsx:533-534) — a user who toggles light on /feed/model gets dark again on /chat and vice versa, and the theme button's 40px effective hit area (26px button + inset:-8px, lines 870-871) is marginally under the 44px target.
  - *Disposition:* REPORTED — not fixed in this branch
  - *Suggested fix:* Read/write theme through ThemeContext (keep the ?theme= override for pixel-verification links), or at minimum have both persist to the same storage key. Bump .dmf-themebtn::after to inset:-10px.
