# F5 (First Five Innings) — ML / RL / Total — Phase 0 Dossier

Audit session date: 2026-07-25. Evidence classes: **VERIFIED** (code read this session, cited
file:line), **INFERRED** (reasoned from verified facts, reasoning stated), **UNKNOWN**.

## Overview

The F5 markets (moneyline, run line ±0.5, total) are priced by the same Python Monte Carlo engine
that prices the full game: `server/MLBAIModel.py` (`project_game` → `MonteCarloEngine.simulate` →
`MarketDerivation.derive`), spawned per-slate by `server/mlbModelRunner.ts` (VERIFIED
mlbModelRunner.ts:1236-1356). **F5 scores are NOT a separate innings-1–5 simulation and NOT a
truncation of the full-game sample** — they are an *independent second sampling pass* from the same
NB-Gamma mixture, with each team's full-game mean scaled by a fixed share constant
`F5_RUN_SHARE = 0.5618` and a ±10 % team-tendency adjustment (VERIFIED MLBAIModel.py:1222-1271).
F5 ML is priced three-way (home / away / push) with the simulated tie rate Bayesian-blended
60/40 toward an empirical 15.07 % push prior (VERIFIED MLBAIModel.py:1284-1302). The F5 run line
is hard-coded at ±0.5 and priced by normalizing P(home wins F5) vs P(away wins F5), i.e. ties are
*excluded* from RL pricing (VERIFIED MLBAIModel.py:1305-1310, 1937-1942). The F5 total is priced at
a **synthetic line — full-game book total × 0.555, snapped to nearest 0.5 — never at the book's
actual F5 total** (VERIFIED MLBAIModel.py:1926-1936). Book F5 lines (FanDuel NJ via Action Network)
are scraped by a separate pipeline (`server/mlbF5NrfiScraper.ts` + `server/ActionNetworkF5NrfiAPI.py`)
that runs only after 7 AM EST, and are never fed back into the model. Results land in the `games`
table's `modelF5*` columns, are served to any logged-out client via the public `games.list` tRPC
procedure, but as of this audit **no live end-user page renders them** — the two F5 UI components
are dead code and only the owner-only `/admin/model-results` page consumes F5 data.

## Data inputs & ingestion

### Model-side inputs (what the F5 numbers are actually computed from)

| Input | Source | Evidence |
|---|---|---|
| Team full-game state `mu`/`variance` | 9-inning expected-runs loop: lineup-weighted PA probs vs *opposing starter* for all 9 innings, bullpen quality multiplier for innings > starter_ip; VarianceModel from Statcast aggregates | VERIFIED MLBAIModel.py:711-741 (build), 725-729 (9-inning loop), 674-698 (variance) |
| Book lines passed to Python | **FG lines only**: `awayML/homeML/bookTotal/overOdds/underOdds/rl_home_spread/RL odds`. **No F5 book field is ever passed to the engine** | VERIFIED mlbModelRunner.ts:1753-1762 |
| Team F5 RS tendency (`away_f5_rs`/`home_f5_rs`) | Runner passes `null` (mlbModelRunner.ts:1899-1900); Python auto-resolves from hardcoded 3-yr table `TEAM_F5_RS` (30 teams, e.g. PIT 2.1912 … ARI 2.9591), fallback league mean 2.484 | VERIFIED MLBAIModel.py:309-343, 2482-2485 |
| Pitcher F5-specific stats | **None.** The schema column `mlb_pitcher_stats.f5RunsAllowedMean` exists (drizzle/schema.ts:1161; census schema-columns.tsv line 398) but is never read or written anywhere in the repo (repo-wide grep, VERIFIED) — dead column |
| Weather/park/umpire/lineup Statcast | Shared with the full-game path; affect F5 only via the full-game `mu` | VERIFIED mlbModelRunner.ts:1857-1931, MLBAIModel.py:2492-2510 |

INFERRED (from the above): F5 is **not starter-only**. Because F5 mu = full-game mu × 0.5618 and
the full-game mu already blends starter innings and bullpen-quality innings (MLBAIModel.py:725-729),
the bullpen's contribution to innings 6–9 is proportionally baked into the F5 projection. A team
with an elite starter and a bad bullpen gets its F5 mean *inflated* by the bullpen share; the only
F5-specific correction is the team-level F5-RS tendency clamped at ±10 %.

### Book-side F5 odds ingestion

- `server/ActionNetworkF5NrfiAPI.py` — single GET to
  `https://api.actionnetwork.com/web/v2/scoreboard/mlb` with
  `periods=event,firstfiveinnings,firstinning`; extracts FanDuel NJ (`book_id="69"`) F5 ML, F5
  spread (value + odds per side), F5 total (value + over/under odds) (VERIFIED
  ActionNetworkF5NrfiAPI.py:43-46, 171-222).
- `server/mlbF5NrfiScraper.ts` (`scrapeAndStoreF5Nrfi`) spawns that script with
  `/usr/bin/python3.11`, matches AN records to DB games by `AWAY@HOME` key (doubleheaders resolved
  by shift-in-order — VERIFIED mlbF5NrfiScraper.ts:193-195), and writes book columns
  `f5AwayML, f5HomeML, f5AwayRunLine, f5AwayRunLineOdds, f5HomeRunLine, f5HomeRunLineOdds,
  f5Total, f5OverOdds, f5UnderOdds` keyed by `games.id` (VERIFIED mlbF5NrfiScraper.ts:210-230).
- **Capture timing**: called in `runMlbCycleOnce()` Step 7 every cycle, but skipped entirely before
  12:00 UTC ("7 AM EST") because FD posts F5 markets the morning of game day; day-ahead seeding is
  deliberately excluded (VERIFIED vsinAutoRefresh.ts:1996-2020, 120-129). One snapshot per cycle,
  overwritten each cycle; there is no odds-history archival and no closing-line capture for F5
  (INFERRED — the update is a plain `UPDATE games SET`, no history insert, mlbF5NrfiScraper.ts:211-230).

### Actual F5 results ingestion

- `server/mlbScoreRefresh.ts` sums MLB Stats API linescore innings 1–5 per side and writes
  `actualF5AwayScore`/`actualF5HomeScore` when a game goes final (requires ≥ 5 innings in the
  linescore) (VERIFIED mlbScoreRefresh.ts:407-430, 660-706).
- `server/mlbOutcomeIngestor.ts` (nightly) writes `actualF5Total` and Brier fields
  (`brierF5Total`, `brierF5Ml`) (VERIFIED mlbOutcomeIngestor.ts:1-48, 209-250).

## Model mechanics

### Simulation and pricing flow (all VERIFIED)

1. Full-game sampling: `home_9/away_9 ~ NBGammaMixture(mu, var)` with 400 000 sims
   (MLBAIModel.py:919-925; SIMULATIONS=400_000 at line 68), extra innings resolved with ghost
   runner for the FG markets only (lines 936-958) — extra innings never touch F5.
2. F5 mu: `mu_f5 = mu_fullgame * 0.5618`, then team F5-RS adjustment
   `adj = clip((team_f5_rs − 2.484)/2.484 × 0.10, −0.10, +0.10)`; `mu_f5 *= (1+adj)`
   (MLBAIModel.py:1222-1267).
3. F5 variance: `var_f5 = max(var_fullgame × 0.5618, mu_f5 + 0.05)` (MLBAIModel.py:1268-1269).
4. Independent re-sampling: `home_f5/away_f5 ~ NBGammaMixture(mu_f5, var_f5)` — integer-valued
   negative-binomial draws (sample() returns `rng.negative_binomial` values,
   MLBAIModel.py:806-812), so ties and half-run cover events are exact (lines 1270-1273).
5. F5 ML three-way: raw `P(home)`, `P(away)`, `P(tie)` from the sample;
   `p_push_adj = 0.6·p_push_raw + 0.4·0.1507`; home/away rescaled so the three sum to 1
   (MLBAIModel.py:1284-1304). Consistency assert: three-way sum within ±0.001 (lines 1319-1322).
6. F5 ML pricing: two-way conditional no-vig on (home|no-push, away|no-push), then multiplied back
   by `(1 − p_push)` to absolute scale; `prob_to_ml` of those absolute probabilities produces
   `f5_ml_home/f5_ml_away` (MLBAIModel.py:1896-1917). **Note**: because odds are taken on
   *absolute* (push-inclusive) probabilities, the two fair MLs overround to less than 100 %
   (sum of implied ≈ 1 − p_push ≈ 0.85) — they price a tie as a *loss*, whereas book F5 ML pushes
   on tie (INFERRED from settlement convention; the code's own comment "push=refund three-way
   pricing" at line 1278-1283 conflicts with this construction).
7. F5 RL: fixed `F5_RL = −0.5`; `p_f5_home_rl = P(margin > 0.5)` (= P(home wins F5)),
   `p_f5_away_rl = P(margin < −0.5)` (= P(away wins F5)); those two are then
   `remove_vig`-normalized against each other and converted to odds
   (MLBAIModel.py:1305-1310, 1937-1942). **Ties are excluded from both sides** — but on a ±0.5
   line a tie is a *win for the +0.5 side*, not a push (the backtest itself acknowledges "push
   impossible with integer scores and 0.5 line", mlbMultiMarketBacktest.ts:537). The stored RL
   cover probabilities are therefore conditional-on-no-tie, not true cover probabilities.
8. F5 total: `f5_ou_line = bookFG_total × 0.555` (or `exp_f5_total` if no book total), snapped to
   nearest 0.5 via `_nearest_half` (round(x·2)/2, MLBAIModel.py:521-523); over/under computed at
   that snapped line with integer-line push mass split 50/50; no-vig; odds
   (MLBAIModel.py:1925-1936).
9. Diagnostics: separate inning-by-inning simulation (I1–I9 weights) is only used for a
   consistency check against the direct F5 sample (|Δ| < 0.50 runs) and for the inning JSON
   columns — it does not feed F5 pricing (MLBAIModel.py:1338-1350, 1398-1411).

### Parameters table

| Name | Value | Meaning | Evidence |
|---|---|---|---|
| `SIMULATIONS` | 400 000 | sims per game (clip 250k–500k) | MLBAIModel.py:68-70, 880-883 |
| seed | 42 (default, runner does not override) | deterministic per input | MLBAIModel.py:2423; mlbModelRunner.ts:1248-1283 (no seed arg) |
| `F5_RUN_SHARE` | **0.5618** | F5 mu share of full-game mu (local constant inside `simulate`) | MLBAIModel.py:1222 |
| F5 total line factor | **0.555** | synthetic F5 O/U line = FG book total × 0.555 (≠ 0.5618, ≠ 0.5595) | MLBAIModel.py:1927 |
| `EMPIRICAL_PRIORS["f5_share"]` | 0.5595 | documented live share — **never read by any code path** (only `f5_push_rate` and `nrfi_rate` are read) | MLBAIModel.py:389; grep lines 1182-1293 |
| `TEAM_F5_RS` | 30 values, 2.0411 (CWS) – 2.9591 (ARI) | 3-yr team F5 runs-scored means | MLBAIModel.py:309-340 |
| `F5_RS_LEAGUE_MEAN` | 2.484 | league mean F5 RS | MLBAIModel.py:341-343 |
| `F5_RS_WEIGHT` | 0.10 | max ±10 % mu adjustment from team tendency | MLBAIModel.py:344, 1226-1233 |
| F5 variance floor | `mu_f5 + 0.05` | keeps NB overdispersed | MLBAIModel.py:1268-1269 |
| NB-Gamma `gamma_shape` | 4.0 | mixture overdispersion; rate multipliers clipped [0.3, 3.0]; per-draw var `max(1.5·mu, mu+0.5)` | MLBAIModel.py:787-811 |
| `EMPIRICAL_PRIORS["f5_push_rate"]` | 0.1507 | empirical F5 tie rate prior | MLBAIModel.py:379, 1293 |
| Push blend weights | 0.6 sim / 0.4 prior | `p_push_adj = 0.6·raw + 0.4·0.1507` | MLBAIModel.py:1292-1294 |
| `F5_RL` | −0.5 | fixed RL spread, direction-agnostic | MLBAIModel.py:1308 |
| `prob_to_ml` clamp | p ∈ [0.001, 0.999] | odds conversion clamp | MLBAIModel.py:561-567 |
| `remove_vig` | p/(p_a+p_b) | proportional normalization | MLBAIModel.py:574-578 |
| HFA in mu | home ×(1+0.35·0.15), away ×(1−0.35·0.08) | `HFA_BASE_WEIGHT=0.35` | MLBAIModel.py:142, 909-911 |
| `STARTER_IP_MEAN/MIN/MAX` | 5.2 / 1.0 / 9.0 | starter innings projection (feeds FG mu, hence F5) | MLBAIModel.py:136-138, 624-644 |
| Runner validation: `modelF5PushPct` range | [0.05, 0.35] | post-write gate | mlbModelRunner.ts:1457-1462 |
| Runner validation: `modelF5PushRaw` range | [0.05, 0.40] + shrink-toward-0.1507 coherence | post-write gate | mlbModelRunner.ts:1467-1489 |
| Backtest `F5_CONFIDENCE_THRESHOLD` | 0.60 | F5 total bet gate in multi-market backtest | mlbMultiMarketBacktest.ts:79-81 |
| Backtest `EMPIRICAL_F5` priors | home ML win 0.4511 / away 0.3982 / push 0.1507; RL away 0.5489 / home 0.4511 | validation-gate priors (not pricing) | MLBAIModel.py:377-385 |

## Projection → DB write path

Single `db.update(games).set({...}).where(eq(games.id, r.db_id))` per game in
`runMlbModelForDate` (VERIFIED mlbModelRunner.ts:2413-2560). Keyed by `games.id`; the row also gets
`modelRunAt = Date.now()`, `publishedToFeed = true`, `publishedModel = true`
(mlbModelRunner.ts:2543-2549). F5 mapping (all VERIFIED mlbModelRunner.ts:2482-2503):

| DB column (games) | Engine field | Scale note |
|---|---|---|
| `modelF5AwayML` / `modelF5HomeML` | `f5_ml_away`/`f5_ml_home` via `fmtMl` (rounded, signed string) | absolute (push-inclusive) fair odds |
| `modelF5AwayScore` / `modelF5HomeScore` | `exp_f5_away_runs`/`exp_f5_home_runs` (toFixed(3)) | decimal(5,2) column → MySQL rounds to 2dp |
| `modelF5Total` | `f5_total_key` — the **synthetic** line (FG book total × 0.555, snapped), NOT the book `f5Total` and NOT `exp_f5_total` | decimal(5,1) |
| `modelF5OverRate` / `modelF5UnderRate` | `p_f5_over`/`p_f5_under` (toFixed(4)) — **0–1 scale** at the synthetic line | column is decimal(5,2) documented "(0-100)" (drizzle/schema.ts:423-426) → stored truncated to 2dp, e.g. 0.5464→0.55 |
| `modelF5OverOdds` / `modelF5UnderOdds` | `f5_over_odds`/`f5_under_odds` | at the synthetic line |
| `modelF5HomeWinPct` / `modelF5AwayWinPct` | `p_f5_home_win × 100` / `p_f5_away_win × 100` | 0–100; push-adjusted absolute (sum ≈ 85) |
| `modelF5PushPct` / `modelF5PushRaw` | `p_f5_push` / `p_f5_push_raw` | 0–1, decimal(6,4) |
| `modelF5HomeRLCoverPct` / `modelF5AwayRLCoverPct` | `p_f5_home_rl × 100` / `p_f5_away_rl × 100` | 0–100; conditional-on-no-tie |
| `modelF5AwayRlOdds` / `modelF5HomeRlOdds` | `f5_rl_away_odds`/`f5_rl_home_odds` | — |
| `modelF5AwayRunLine: '-0.5'`, `modelF5HomeRunLine: '+0.5'` | hardcoded literals | **columns do not exist** — see finding F-2 |

**F-2 detail (VERIFIED this session):** `modelF5AwayRunLine`/`modelF5HomeRunLine` appear in no
schema (drizzle/schema.ts:398-468 has only book `f5AwayRunLine`/`f5HomeRunLine`; census
schema-columns.tsv has no such columns). Drizzle v0.45.2's `buildUpdateSet` iterates
`Object.keys(tableColumns)` and silently drops set-object keys that aren't table columns
(VERIFIED node_modules/drizzle-orm/mysql-core/dialect.cjs:111-129), and the `.set()` type accepted
the extra keys (probe compile passed, this session). So the model's RL side labels are silently
discarded: **nothing in the DB records which side the model's F5 RL odds refer to.** Note the
dropped literals (`away='-0.5'`, `home='+0.5'`) also contradict the backtest's fixed convention
(home −0.5 / away +0.5, mlbMultiMarketBacktest.ts:534-537, 557, 576).

Invalidation path: when the FG RL sign-flip guard fires, all FG model fields are nulled atomically
— **the modelF5\* fields are NOT in the null list**, so stale F5 numbers survive an FG
invalidation until the re-run completes (VERIFIED mlbModelRunner.ts:2368-2398; INFERRED
consequence — the immediate targeted re-run at 2600-2622 normally closes this window quickly).

## Exposure (API + UI)

- **Public API**: `games.list` (tRPC, `publicProcedure`) returns every `modelF5*`, `f5*` book, and
  F5 backtest column for MLB rows; the sport-strip helper only removes them from non-MLB rows
  (VERIFIED routers.ts:104-148, 311-357).
- **Owner API**: `mlbSchedule.getF5EdgeLeaderboard` (`ownerProcedure`) — F5 ML edge board
  comparing `modelF5AwayWinPct/modelF5HomeWinPct` (absolute, push-inclusive, 0–100) against
  two-way no-vig book implied from `f5AwayML/f5HomeML` (sums to 100) — a systematic ≈ −7.5 pp per
  side mismatch (VERIFIED routers/mlbSchedule.ts:697-860; INFERRED bias size = push mass/2).
  Outcome stats filter on `f5MlResult`/`f5MlCorrect`, which have **no writer anywhere in the
  repo** (VERIFIED repo grep — only readers at routers/mlbSchedule.ts:719-720, 1026), so
  `winRateOnPositiveEdge` is permanently null/PENDING unless a DB-only historical writer existed
  (UNKNOWN — census question). Also `mlbSchedule.getBrierTrend` with markets `f5Ml`/`f5Total`
  (routers/mlbSchedule.ts:994-1090).
- **Live UI**: the current authenticated feed (`/feed/model/:sport` → `DimeModelFeed.tsx`) renders
  **zero F5 markets** (VERIFIED — grep for f5/F5 in DimeModelFeed.tsx returns nothing). Owner page
  `/admin/model-results` (TheModelResults.tsx) embeds the F5 edge board
  (TheModelResults.tsx:1048); `/admin/f5-edge` now just redirects there (App.tsx:322-324).
- **Dead UI** (both fully wired to F5 columns but unmounted):
  - `client/src/components/MlbF5NrfiCard.tsx` — imported by nothing (VERIFIED repo grep).
  - `client/src/pages/ModelProjections.tsx` (the "CHEAT SHEETS" f5nrfi tab hosting
    `MlbCheatSheetCard` + `CheatSheetView`/`F5GameCardV3`) — imported by nothing since the Dime
    feed migration (VERIFIED grep; App.tsx routes `/projections` → `/feed/model/mlb`).
  - These dead components contain live-looking bugs that would matter if remounted: both edge
    helpers divide the model pct by 100 (MlbF5NrfiCard.tsx:132-150; MlbCheatSheetCard.tsx:172-185,
    1110-1122) while `modelF5OverRate/UnderRate` are stored 0–1 → F5 total "edge" computes with
    p≈0.005 instead of 0.55 (VERIFIED call sites MlbF5NrfiCard.tsx:353-354,
    MlbCheatSheetCard.tsx:990-991, 1500-1505); and the F5 RL edge pairs the tie-excluded model
    cover pct with whichever side FD happens to lay −0.5 (MlbF5NrfiCard.tsx:429-430,
    MlbCheatSheetCard.tsx:809-810, 1451).

## Scheduling & triggers

| Trigger | Cadence | What it runs | Evidence |
|---|---|---|---|
| `startMlbModelSyncScheduler()` | on boot + every 5 min, 24/7 (+2-min watchdog that self-heals) | `runMlbModelForDate(today)` then `(tomorrow)`; idempotent via `modelRunAt`-same-date guard | VERIFIED mlbModelRunner.ts:2673, 2799-2825, 1586-1601; registered in _core/index.ts:877 behind `DISABLE_BACKGROUND_JOBS` guard (index.ts:840-877) |
| `runMlbCycleOnce()` Step 6 | on boot + every 5 min in-process (`MLB_INTERVAL_MS`, vsinAutoRefresh.ts:1361, 2096-2101), **or** GitHub Actions `cron-mlb-cycle.yml` `*/5 * * * *` → POST `/api/cron/mlb-cycle` when background jobs are disabled | fallback `runMlbModelForDate(today, tomorrow)` | VERIFIED vsinAutoRefresh.ts:1859-1884; cronRoutes.ts:49-83; .github/workflows/cron-mlb-cycle.yml:36 |
| `runMlbCycleOnce()` Step 7 | same cycle, gated `utcHour >= 12` | `scrapeAndStoreF5Nrfi(today)` — book F5/NRFI odds | VERIFIED vsinAutoRefresh.ts:1996-2020 |
| LineupWatcher (MLBCycle Step 5) | each cycle, on lineup change | `runMlbModelForDate(dateStr)` | VERIFIED mlbLineupsWatcher.ts:578-600 |
| `tRPC mlbModel.forceRerun` | manual, owner | `runMlbModelForDate(date, {forceRerun:true})` | VERIFIED routers.ts:995-1008 |
| Score refresh (MLBCycle Step 1) | every cycle | writes `actualF5AwayScore/actualF5HomeScore` on final | VERIFIED mlbScoreRefresh.ts:660-706 |
| Multi-market backtest | on FINAL transition within a cycle | `runMultiMarketBacktest(gameId)` → grades F5 ML/RL/Total into `mlb_game_backtest` | VERIFIED vsinAutoRefresh.ts:1949-1994 |
| `startMlbOutcomeAndDriftScheduler()` | nightly 12:30 AM PST + monthly 1st 3 AM PST | outcome ingest (`actualF5Total`, `brierF5Total`, `brierF5Ml`) → `checkF5ShareDrift` → possible auto-recalibration patching MLBAIModel.py | VERIFIED _core/index.ts:873, mlbOutcomeAndDriftScheduler.ts:1-35 |

Timing consequence (INFERRED from the verified gates): the model typically runs **before** F5 book
odds exist (model needs only FG lines + pitchers, runs 24/7 incl. day-ahead; F5 scrape starts at
7 AM EST same-day), and the `modelRunAt` guard prevents any re-run when F5 book lines later arrive.
So `modelF5Total`/`modelF5OverRate` are essentially never aligned to the book's `f5Total` line
except by coincidence of the ×0.555 heuristic.

Two open deployment questions (UNKNOWN): whether `DISABLE_BACKGROUND_JOBS` is set on the Railway
production service (decides in-process vs GH-Actions triggering — cronRoutes.ts:5-9), and whether
`cron-mlb-cycle.yml` is enabled in the Actions UI (its header says leave disabled until Manus is
retired, cron-mlb-cycle.yml:4-11). The stale comment at cronRoutes.ts:23-27 claiming Python spawn
fails on Railway predates the Dockerfile fix (Dockerfile:4-19 installs Debian python3 + numpy/scipy).

## Patch history relevant to this market

- **Commit 5351941a** — added the 16 F5 fields to `project_game()`'s return dict (they were computed
  but not returned), fixed the F5/NRFI scraper date format (YYYYMMDD). Part of the live path now
  (VERIFIED git log -S modelF5AwayRunLine; commit message read this session).
- **2026-04-14 "3yr v2" calibration** — F5_RUN_SHARE 0.5311→0.5618, inning weights updated,
  three-way push pricing (v2.1) introduced (`modelF5PushPct`/`modelF5PushRaw` columns). Live
  (VERIFIED constants + comments MLBAIModel.py:1215-1222, 1277-1283; mlbModelRunner.ts:2494-2496).
- **2026-05-10 "P3-C" live-2026 recalibration** — `f5_share` 0.5595, `f5_mean` 4.881 written into
  `EMPIRICAL_PRIORS` (MLBAIModel.py:389-397). Historical only in effect: `f5_share` is never read
  (VERIFIED grep — only `f5_push_rate`/`nrfi_rate` are read at 1182-1293).
- **`scripts/backfillF5Push2026.mts`** — one-off backfill (2026-03-25→04-13) that re-ran the
  standard `runMlbModelForDate` per date to populate NULL `modelF5PushPct` rows; used the live
  engine, so no divergent math (VERIFIED scripts/backfillF5Push2026.mts:1-45).
  `scripts/debugF5Push.mts` — diagnostic only.
- **Auto-recalibration loop (live but latent)** — `mlbDriftDetector.triggerRecalibration` spawns
  `server/scripts/runMlbBacktest2.py` and then regex-patches `EMPIRICAL_PRIORS` in MLBAIModel.py
  *in the running container* (backup to `.bak`; not committed to git → reverted on redeploy)
  (VERIFIED mlbDriftDetector.ts:729-825). Two F5-relevant defects in that loop:
  1. It patches `f5_share` (dead constant) but **cannot patch the live `F5_RUN_SHARE = 0.5618`**,
     which is a bare local assignment outside the dict the regex targets (VERIFIED
     mlbDriftDetector.ts:762-784 vs MLBAIModel.py:1222) — so F5-share drift detection can never
     actually correct the F5 simulation.
  2. Its `f5_push_rate` input is **not a push rate**: runMlbBacktest2.py computes it as
     NO_ACTION/(n+NO_ACTION) for market key `"f5_total"` (runMlbBacktest2.py:225-230) — a market
     name that `mlbMultiMarketBacktest` never writes (it writes `f5_over`/`f5_under`,
     mlbMultiMarketBacktest.ts:97-98) — so the lookup returns {} and `safe_div(0,0,0.05)` yields
     the **0.05 default**, which would overwrite the live 0.1507 push prior on the first
     successful recalibration (VERIFIED both files; whether a recalibration has ever fired in
     production is UNKNOWN — `mlb_model_learning_log` census question).
- One-off rerun scripts (`forceRerunJune17/18/19.ts`, `runJune13Mlb.ts`, `rerunSFATLG2*.ts`) all
  call the standard `runMlbModelForDate(force)` — no divergent F5 math (VERIFIED imports/calls).

## Open questions (UNKNOWN)

1. Has `f5MlResult`/`f5RlResult`/`f5TotalResult`/`f5*Correct`/`f5BacktestRunAt` ever been
   populated? No writer exists in the current tree; the F5 edge leaderboard's win-rate summary
   depends on them (routers/mlbSchedule.ts:830-853). → DB census: count non-null per column.
2. Has the auto-recalibration ever fired (rows in `mlb_model_learning_log` with triggerReason
   DRIFT_DETECTED/SCHEDULED/MANUAL)? If yes, production `EMPIRICAL_PRIORS['f5_push_rate']` may have
   been 0.05 between that run and the next redeploy. → DB census + `modelF5PushPct` distribution
   over time (blend floor 0.6·raw + 0.4·0.05 vs 0.4·0.1507 is detectable).
3. Is `DISABLE_BACKGROUND_JOBS` set on Railway, and is `cron-mlb-cycle.yml` enabled? Determines
   which of the two 5-minute schedulers actually drives the pipeline.
4. What are the actual stored values of `modelF5OverRate`/`modelF5UnderRate` (expected ≈ 0.35–0.65
   truncated to 2dp)? Confirms the 0–1-in-a-(5,2)-column defect and quantifies precision loss.
5. Does FanDuel NJ settle F5 ML as push-on-tie (assumed) — needed to decide whether the model's
   absolute-probability fair ML (F-5) or a conditional fair ML is correct.
6. How often does `round(bookFG_total × 0.555 × 2)/2` differ from the scraped `f5Total`? (e.g.
   8.5→4.5 while FD posts 4.0/5.0). → DB census join.
7. Was `mlb_pitcher_stats.f5RunsAllowedMean` ever populated by a since-deleted job? Currently
   read/written nowhere.

## Finding candidates

| # | Sev | Title | Evidence |
|---|---|---|---|
| F-1 | P1 | F5 RL cover probabilities exclude ties, so the +0.5 side is systematically understated (~15 pp of outcome mass) and the −0.5 side overstated; backtest edges and (dead) UI edges inherit the bias | MLBAIModel.py:1305-1310, 1937-1942 (ties dropped, then re-normalized); mlbMultiMarketBacktest.ts:534-582 (grades home −0.5 win iff margin>0, away +0.5 win iff margin≤0 vs tie-excluded model probs) |
| F-2 | P1 | Runner writes `modelF5AwayRunLine`/`modelF5HomeRunLine` to nonexistent columns; drizzle silently drops them, so the model's F5 RL side labels are never persisted — and the dropped labels ('away −0.5/home +0.5') contradict the backtest convention (home −0.5/away +0.5) | mlbModelRunner.ts:2497-2498; drizzle/schema.ts:398-468 (no such columns); node_modules/drizzle-orm/mysql-core/dialect.cjs:111-129 (silent drop); mlbMultiMarketBacktest.ts:557,576 |
| F-3 | P1 | `modelF5OverRate`/`modelF5UnderRate` written 0–1 into a decimal(5,2) column documented 0–100: precision truncated to 2 dp, brier/UI consumers treat as 0–100 → `brierF5Total` corrupted (p≈0.005) and both cheat-sheet UIs compute F5 total edges ~−50 pp | mlbModelRunner.ts:2490-2491 (0–1 write); drizzle/schema.ts:423-426 ("0-100", decimal(5,2)); mlbOutcomeIngestor.ts:156-166 + 218 (÷100 again); MlbCheatSheetCard.tsx:172-185, 990-991; MlbF5NrfiCard.tsx:132-150, 353-354 |
| F-4 | P1 | F5 total is priced at a synthetic line (FG book total × 0.555, snapped) that is never reconciled with the book's `f5Total`; backtest and Brier grade the model's probability against the *book* line, so every F5 total metric mixes two different lines; model never re-runs when F5 book lines arrive (7 AM EST gate + modelRunAt guard) | MLBAIModel.py:1927-1928; mlbModelRunner.ts:2487 (modelF5Total = synthetic), 1594-1601 (no re-run); vsinAutoRefresh.ts:1996-2020 (scrape gate); mlbMultiMarketBacktest.ts:597-638 and mlbOutcomeIngestor.ts:209-220 (graded at book line) |
| F-5 | P2 | F5 ML fair odds/win pcts are absolute (tie counted as loss; implied sum ≈ 85 %) but the market pushes on tie and the book comparison is two-way no-vig (sum 100 %) — every F5 ML edge (leaderboard, backtest, dead UIs) carries a built-in ≈ −7.5 pp per side | MLBAIModel.py:1911-1917 (absolute no-vig); routers/mlbSchedule.ts:772-802 (model 0–100 absolute vs two-way no-vig); mlbMultiMarketBacktest.ts:483-533 |
| F-6 | P2 | Latent auto-recalibration would poison the live F5 push prior to 0.05: recal script reads market key `f5_total` that is never written and defaults `f5_push_rate` to 0.05, which `migrateCalibrationConstants` would patch over the live 0.1507; separately the drift loop can never fix the constant it monitors (`F5_RUN_SHARE` unpatchable) | server/scripts/runMlbBacktest2.py:225-230; mlbMultiMarketBacktest.ts:93-98; mlbDriftDetector.ts:762-800; MLBAIModel.py:1222,1293 |
| F-7 | P2 | F5 outcome columns `f5MlResult/f5RlResult/f5TotalResult/f5*Correct/f5BacktestRunAt` have no writer — the F5 edge leaderboard's outcome summary can never populate from the current code | repo-wide grep (readers only: routers/mlbSchedule.ts:719-720, 830-853; drizzle/schema.ts:455-468) |
| F-8 | P3 | All F5 model/book fields ship to any client via public `games.list` while no live page renders them; the two F5 UI components (MlbF5NrfiCard, ModelProjections/CheatSheetView) are dead code containing the F-1/F-3 consumer bugs | routers.ts:104-148, 311-357; grep: no imports of MlbF5NrfiCard/ModelProjections; DimeModelFeed.tsx (no F5) |
| F-9 | P3 | FG RL sign-flip invalidation nulls all FG model fields but leaves stale `modelF5*` values in place until the targeted re-run lands | mlbModelRunner.ts:2368-2398 (null list omits F5), 2600-2622 (re-run) |
| F-10 | P3 | Three inconsistent F5 share constants coexist (sim 0.5618, total-line 0.555, documented live share 0.5595) — the total line and the simulated distribution are mutually inconsistent by construction | MLBAIModel.py:1222, 1927, 389 |
