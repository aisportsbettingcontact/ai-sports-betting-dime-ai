# FINDINGS — Dime AI UI Forensic Audit, 2026-07-31

Repo `aisportsbettingcontact/ai-sports-betting-dime-ai` · branch `fix/mlb-verify-dynamic-total` · commit `fcb85dddf25b5730ab9539bcc68c6e9a608a85f0`.

**Judged against the repo's own locked law**, which has evolved past the audit brief: `dime-ai/THREE-COLOR-LAW.md` v2/v3 supersedes MASTER.md's color table, and the single-font mandate ([index.css:7-12](../../client/src/index.css#L7-L12), owner-approved 2026-07-24) **retires IBM Plex Mono** — the brief's "Plex Mono for numeric content" clause is no longer law. Where a finding depends on that divergence it is tagged `law-divergence`.

All rendered evidence captured from the dev server (web-only boot, `DISABLE_BACKGROUND_JOBS=1`, real DB reads) at commit above. Auth for sub-768px surfaces was unlocked by patching only the `appUsers.me` response client-side; all data shown is real server data. `evidence/` paths are relative to this directory. Census JSONs (`evidence/census-*.json`) hold the full computed-style measurements; `evidence/probes.json` holds geometry/keyboard/tabular probes.

Format: `ID | surface | lens | SEV | status | evidence | standard violated | fix direction`

## SEV-1 — Apple would block ship

- **DIME-UI-001** | landing `/` final CTA · 1280 · dark | Lens 3 | SEV-1 | VERIFIED | [FinalCTA.tsx:15](../../client/src/pages/dime/landing/components/FinalCTA.tsx#L15) renders `Get access · dıme` as `.btn--mint`; computed: white `.wordmark` text on `#45E0A8` fill = **1.68:1** (needs 4.5) — `evidence/landing-final-cta.png`, `evidence/census-landing-1280-dark.json` contrastFails[0] | WCAG 1.4.3 on the page's primary conversion CTA; law: mint surfaces take black ink (`--primary-foreground: #000`) | Render the wordmark in black (or the black-ink brand lockup) when placed on a mint fill.
- **DIME-UI-002** — **PULLED, replaced by SEV-4 residuals** (see DIME-UI-043/-044 and the pulled list). A settled re-measurement (focus + 350ms wait) shows a mint 3px ring on BOTH badge variants; the original keyboard-walk reading sampled mid-transition.
- **DIME-UI-003** | all Radix overlays incl. the LINEUPS dialog on the feed | Lens 5/7 | SEV-1 | VERIFIED | `tw-animate-css` keyframes (`animate-in`/`animate-out`, accordion) drive [ui/dialog.tsx:82,127](../../client/src/components/ui/dialog.tsx#L82), [ui/sheet.tsx:39,61](../../client/src/components/ui/sheet.tsx#L39), [ui/popover.tsx:31](../../client/src/components/ui/popover.tsx#L31), [ui/drawer.tsx:38](../../client/src/components/ui/drawer.tsx#L38); the reduced-motion kill list at [index.css:314-319](../../client/src/index.css#L314-L319) covers only `animate-pulse/spin/bounce/ping` — `animate-in/out` run regardless of `prefers-reduced-motion` | MASTER.md motion law "**Always** respect prefers-reduced-motion"; WCAG 2.3.3 | Add `animate-in`/`animate-out`/`animate-accordion-*`/`animate-caret-blink` to the index.css reduced-motion kill block (or wrap tw-animate usage in a motion-safe variant).
- **DIME-UI-004** | `/feed/model/mlb` · **390 · dark+light** (pregame panel) | Lens 2/6 | SEV-1 | VERIFIED | Measured DOM at 390px: card 358px wide, `.pregame-pitchers` grid resolves `grid-template-columns: 145px 145px`; away pitcher at (34,130), home pitcher at (179,**266**) — one full row down, diagonal — and the LINEUPS chip (64×32, `position: relative`) sits at x=292 hugging the card edge between them (`evidence/probes.json` → feed-390-geometry; `evidence/feed-390-dark.png` vs the intact side-by-side layout at 767px `evidence/feed-767-dark.png`). The compact-card overlay tier in [ProjectionCard.css:292-317](../../client/src/components/projections/ProjectionCard.css#L292-L317) (absolute-centered chip over a 2-col grid) is not what renders — the chip stays in flow and wraps, so ~136px of vertical space is wasted and the "Expected" labels land at different heights | Apple craft "nothing is random"; the layout is visibly broken at the most common phone width (390 = iPhone 14/15) | Make the ≤520px card tier actually apply (or design an explicit stacked tier); acceptance is side-by-side pitchers with a centered chip, or a deliberate vertical stack — not a diagonal.
- **DIME-UI-005** | `/bet-tracker`, `/betting-splits`, landing captions · dark | Lens 3/7 | SEV-1 | VERIFIED | `--text-muted: #6E6E6E` ([index.css:191](../../client/src/index.css#L191), self-documented "4.1:1 on black") is applied to 10–11px labels: tracker `bt-label`/`bt-faint` measured **3.88:1** on `#0A0A0A` cards ("Total Bets", "Date", "RECORD", "on $0.00 risked"), splits separators 4.12:1, landing captions on `#141414` **3.61:1** (`evidence/census-tracker-1280-dark.json`, `census-splits-1280-dark.json`, `census-landing-1280-dark.json`) | WCAG 1.4.3 — 4.5:1 required at these sizes; primary-content labels | Introduce a ≥4.5:1 muted tier for sub-18px text (≈`#767676` on black, as the light theme already does at [index.css:231](../../client/src/index.css#L231)) and reserve `#6E6E6E` for ≥24px or decorative text.

## SEV-2 — a design lead rejects the review

- **DIME-UI-006** | shared `ui/button.tsx` (every consumer surface) | Lens 4 | SEV-2 | VERIFIED | [button.tsx:7-30](../../client/src/components/ui/button.tsx#L7-L30): zero `:active` press state on all six variants; `default`/`destructive`/`secondary` have no `hover:` either; base uses bare `transition-all` (150ms Tailwind default curve, off the 160ms brand curve); two malformed dangling `dark:` prefixes at lines 16 and 20 emit nothing (botched edit); sizes `sm`/`icon-sm` = 32px, default = 36px — under the 44px touch floor | Apple "respond on pointer-down"; emil-design-eng press feedback; brand motion law | Rebuild the shared button: press scale on `:active`, hover for all variants, brand curve/duration, ≥44px touch sizes on touch surfaces, delete the dangling `dark:` fragments.
- **DIME-UI-007** | motion system, app-wide | Lens 5 | SEV-2 | VERIFIED | 86 × `transition-all` (56 with no duration → 150ms default curve), 114 × bare `transition-colors`, 19 inline `ease` strings, 4 × `ease-linear` ([ui/sidebar.tsx:225,239,302,416](../../client/src/components/ui/sidebar.tsx#L225)), `ease-in-out` on [ui/sheet.tsx:61](../../client/src/components/ui/sheet.tsx#L61), `duration-[320ms] ease-out` in admin panels — ~197 interactive declarations off the single-curve law (counts in motion sweep; brand-legal 160ms/cubic-bezier(.16,1,.3,1) covers only 85/107 CSS literals) | MASTER.md "One curve everywhere: 160ms cubic-bezier(0.16,1,0.3,1)" | Define `--dime-t`/`--dime-ease` globally and sweep every `transition-*` utility onto them; ban bare `transition-all` by lint.
- **DIME-UI-008** | `/feed` card edge badge system | Lens 4/8 | SEV-2 | VERIFIED (H1) | [EdgeIndicator.tsx:15-21,49-75](../../client/src/components/projections/EdgeIndicator.tsx#L15-L75): BET and WATCH differ only by a 14px icon swap (TrendingUp vs Eye) — the visible label says "Edge" in both, "Bet"/"Watch" exist only in aria-label; NO_EDGE renders a differently-shaped "ROI +x%" badge (no icon, different min-height 1.75rem vs 2rem, border vs mint rail); the → arrow appears only on multi-edge cards ([ProjectionCard.tsx:126](../../client/src/components/projections/ProjectionCard.tsx#L126)) and restyles per variant. Rendered proof: `evidence/feed-1280-dark.png` (EDGE +5.6% w/ glyph beside ROI +6.0% plain in one row) | System coherence; "same concept, same treatment" | One badge anatomy with a recommendation slot: identical geometry across BET/WATCH/NO_EDGE; make the WATCH state legible in text, not icon-only.
- **DIME-UI-009** | `/feed` mobile hierarchy · 390 | Lens 6 | SEV-2 | VERIFIED (H3) | First-viewport order measured at 390×844: date nav (y124) → league bar (y171) → matchup (y259–312) → pitcher block (y359–583, inflated by DIME-UI-004) → model edge label at **y648** — the product's core value is the last element above the fold and the second card's edge is never visible without scrolling (`evidence/probes.json` → firstViewportOrder) | Product intent: "the model recommendation and edge are the product"; grid-template-areas [ProjectionCard.css:26-29](../../client/src/components/projections/ProjectionCard.css#L26-L29) place pregame above summary at every width | Move the edge/summary row above the pitcher block on scheduled cards (or compress pregame) so the verdict is visible in the first card-height.
- **DIME-UI-010** | mobile floating nav | Lens 6 | SEV-2 | VERIFIED (H4) | [mobileFloatingNav.css:149-170](../../client/src/features/mobileNav/mobileFloatingNav.css#L149-L170) current-page pill: inverse fill + 600 + 4px mint dot; [:202-216](../../client/src/features/mobileNav/mobileFloatingNav.css#L202-L216) Chat pill: always-mint fill, 48px/700/13px, shadow — the strongest element in the nav is a *destination you are not on*, and its own current-state deletes the dot ([:256-258]) and swaps content to a placeholder ("3,000 credits", [MobileFloatingNav.tsx:167-171](../../client/src/features/mobileNav/MobileFloatingNav.tsx#L167-L171)) | One-accent law ("mint = signal: active nav"); wayfinding — two competing "current" signals | Reserve mint fill for the active destination (or de-mint the chat pill to outline when inactive); replace the hardcoded credits placeholder before launch. `possibly-intentional` (owner directive 2026-07-29 noted in code).
- **DIME-UI-011** | typography scale, feed + shell | Lens 1 | SEV-2 | VERIFIED | Census at 1280: **18 distinct rendered font sizes** on one viewport, including near-collisions from two coexisting sizing systems — fluid tokens (12.48, 12.898, 13.5168, 15.0272, 16.64, 19.2256px = clamp()×`--scale` math) alongside fixed px (11, 13, 15, 17, 18px) and rem-scaled html (`evidence/census-feed-1280-dark.json` distinctFontSizes); GameCard.tsx alone carries 63 distinct hand-tuned `clamp()` triples + magic multipliers (`* 0.22`, `* 0.018`, [GameCard.tsx:186-197](../../client/src/components/GameCard.tsx#L186-L197)) | Type scale discipline — sizes should form a scale, not an accretion | Collapse to one fluid scale (the `--fs-*` tokens at [index.css:94-104](../../client/src/index.css#L94-L104)) and forbid per-element clamp math.
- **DIME-UI-012** | keyboard order, feed | Lens 7 | SEV-2 | VERIFIED | Keyboard walk (`evidence/probes.json`): `summary-carousel__track` AND `summary__viewport` DIVs are both tab stops on every card — 2 non-interactive scroll containers per card, so 28 tabs traverse ~6 cards; no skip link; the shell sidebar was never reached in 28 tabs | WCAG 2.4.1/2.4.3; keyboard ergonomics | One tabbable scroll container per carousel (or `tabindex="-1"` + arrow-key pattern), add a skip-to-content link.
- **DIME-UI-013** | form inputs, tracker + chat | Lens 7 | SEV-2 | VERIFIED | Census unnamedIconButtons: 4 × `bt-input` (Add-Bet odds/risk/win fields) and `dc-composer-input` have no accessible name (no label/aria-label/placeholder-only at capture) — `evidence/census-tracker-1280-dark.json` | WCAG 4.1.2 / 1.3.1 | Label every input (visible label or aria-label tied to the field's purpose).
- **DIME-UI-014** | mint rationing, product surfaces | Lens 3/6 | SEV-2 | VERIFIED | Mint appears on 89 elements per feed viewport (71 text + 18 fills), 94 on splits (`census-*.json` mint counts). On a scheduled card the only solid-mint fill is **LINEUPS** — a secondary reference lookup — while the model's verdict gets a tint ([ProjectionCard.css:237-259](../../client/src/components/projections/ProjectionCard.css#L237-L259) vs EdgeIndicator surface tokens); rendered: `evidence/feed-390-dark.png` | Law: "if it isn't signal, it isn't mint"; "edge values pop because everything else is grey — protect that contrast" | Demote LINEUPS to outline/ghost; let the edge badge own the only mint fill on a card.
- **DIME-UI-015** | dark theme muted tier duality | Lens 3 | SEV-2 | VERIFIED | Two sanctioned mint-on-light values ship: `#0FA36B` ([ProjectionCard.css:181,674,942] light blocks) and `#0B8557` ([dime-mobile.css:24](../../client/src/styles/dime-mobile.css#L24), documented "the law's value" at :624) — and [dimeModelFeed.test.ts:180-184](../../client/src/pages/dimeModelFeed.test.ts#L180-L184) *bans* `#0FA36B` as "legacy-mint" (case-sensitively, so it doesn't even fire). Two guards in one tree take opposite positions on the same hex | Token discipline; single source of truth | Owner ruling on the canonical mint-on-light hex; fix or delete the contradictory test guard.
- **DIME-UI-016** | hex-literal debt | Lens 9/3 | SEV-2 | VERIFIED | 1,557 hardcoded hex literals in 57 non-test files (excl. index.css); `#45E0A8` pasted raw **463×** and as arbitrary Tailwind (`text-[#45E0A8]` etc.) 178× despite `--primary` existing; top offender [PublishProjections.tsx](../../client/src/pages/PublishProjections.tsx) = 281. The repo self-documents this as **X-HEX-EPIDEMIC** and freezes it via [hexLiteralCeilings.json](../../client/src/hexLiteralCeilings.json) (ceiling sum 1,760; 59 of 66 files at zero headroom) | Three-layer token discipline | Continue the ratchet burn-down; start with the two files already at 0 usage but still holding allowances (BettingSplits.tsx 11→0, TrendsPage.tsx 4→0) and the 178 arbitrary-mint utilities (mechanical `text-[#45E0A8]` → `text-primary`).
- **DIME-UI-017** | sibling-card divergence | Lens 9 | SEV-2 | VERIFIED | Same bar-segment block exists 3× with drifted values: label `100 %` (space) + `borderRadius:"9999px"` + no transition in [GameCard.tsx:753-815](../../client/src/components/GameCard.tsx#L753-L815) vs `100%` + `4`px radius + brand transition in [BettingSplitsPanel.tsx:452-510](../../client/src/components/BettingSplitsPanel.tsx#L452-L510) vs `100%` + `9999px` at [:796-855]; renamed-identical helpers (`isUnusable`/`isUnusableBarColor`, `tooSimilar`/`areColorsTooSimilar` — bodies byte-identical); 6 independent `TeamLogo` implementations, 6 × `fmtOdds`, 5 × `toNum`, 3 × `getEdgeColor` | Copy-paste divergence — the definitive provenance fingerprint | Extract shared helpers/components; normalize the three bar blocks to one.
- **DIME-UI-018** | styling paradigms | Lens 9 | SEV-2 | VERIFIED | Three sibling card components, three systems: GameCard.tsx **170 inline styles**, ProjectionCard = BEM + 1,046-line CSS file, BetTracker = Tailwind (491 classes); 16 files carry >10 inline `style={{` each; 853 arbitrary Tailwind values (~600 excluding vendored ui/) | One-convention rule; maintainability | Pick the ProjectionCard convention (tokens + CSS) as canonical for cards; migrate on touch.
- **DIME-UI-019** | skeleton/loading states, feed | Lens 5 | SEV-2 | VERIFIED | Feed skeleton renders only the first grid column with fixed px bars (90/170/150/200px) inside a `250px 1fr 240px` grid — markets column and right rail absent, so the card reflows on resolve ([DimeModelFeed.tsx:280-293](../../client/src/pages/DimeModelFeed.tsx#L280-L293) vs :1407); `.dmf-skel` has **no animation** while every other skeleton pulses; shell preview shows the mismatch rendered (`evidence/probe-feed-preview-1280-dark.png` vs `evidence/feed-1280-dark.png`). Measured CLS on warm load was ~0.0002, so the cost is the visible skeleton→content jump, not scroll shift | Skeletons must match loaded dimensions; one loading grammar | Derive skeleton geometry from the loaded card template (same grid), pick one pulse treatment app-wide.
- **DIME-UI-020** | tracker header · 1280 | Lens 2/8 | SEV-2 | VERIFIED | Owner/user toggle clips its own label to "OWNI" at 1280 (`evidence/tracker-1280-dark.png`, top-right); adjacent stat tiles show `WP% 0.0%` beside `ROI% 0%` — two percent formats in one row | Truncation of a control's own label; formatting consistency | Give the header row a min-width/overflow strategy; one percent formatter (define decimals per metric).
- **DIME-UI-021** | tracker Add-Bet vs feed data | Lens 8 | SEV-2 | VERIFIED | Same rendered instant: feed lists 15 MLB games for 07/31 (`evidence/feed-1280-dark.png`) while Add-Bet's GAME field says "No MLB games on 07/31/2026" (`evidence/tracker-1280-dark.png`) — two sources of truth disagree on a user-visible fact | Transparency brand promise; state coherence | Point the Add-Bet game picker at the same slate the feed renders (or surface the divergence honestly).
- **DIME-UI-022** | emoji + unicode-styling as UI (admin/owner surfaces) | Lens 9/4 | SEV-2 | VERIFIED | 56 emoji lines in 13 files: `🚫/⚡/🔐` select labels [SecurityEvents.tsx:210-212](../../client/src/pages/SecurityEvents.tsx#L210-L212), `🟢🟡🔴` chart legend + `📋/📊` toggles [TheModelResults.tsx:642,1413](../../client/src/pages/TheModelResults.tsx#L642), `🥅` [PublishProjections.tsx:302](../../client/src/pages/PublishProjections.tsx#L302), `👑/💀` [BetCalendar.tsx:631,647](../../client/src/components/BetCalendar.tsx#L631), mathematical-bold Unicode as typography [SecurityEvents.tsx:182](../../client/src/pages/SecurityEvents.tsx#L182) — while lucide is the app's icon system | MASTER.md "Emojis as icons" anti-pattern | Replace with lucide equivalents; admin-only scope keeps this out of SEV-1.
- **DIME-UI-023** | interruptibility, disclosure surfaces | Lens 5 | SEV-2 | VERIFIED | Every Radix overlay + the splits `.ohp-body` accordion animate via **keyframes** (restart-from-zero, cannot be grabbed/reversed): [dime-mobile.css:430-441](../../client/src/styles/dime-mobile.css#L430-L441), tw-animate `enter`/`exit`; only the chat drawer uses the interruptible spring ([lib/springSettle.ts](../../client/src/lib/springSettle.ts) — critically damped, retargetable — the repo's own best practice, unused elsewhere) | Apple interruptibility ("never lock out input"); emil: transitions over keyframes for dynamic UI | Convert overlay/accordion motion to transitions (or the existing springSettle), keyframes only for one-shot decorations.
- **DIME-UI-024** | hover-only affordances | Lens 4/7 | SEV-2 | VERIFIED | [ClaudeAssistant.tsx:79](../../client/src/pages/ClaudeAssistant.tsx#L79) copy button `opacity-0 group-hover:opacity-100` with **no focus-visible reveal** — keyboard-focusable but invisible while focused; [MlbLast5Panel.tsx:325](../../client/src/components/MlbLast5Panel.tsx#L325) same pattern, invisible on touch; ~178 of 195 `hover:` lines sit in files with zero `active:` | WCAG 2.4.7; MASTER.md "hover-only affordances" anti-pattern | Pair every hover reveal with `focus-visible:` and a touch fallback.

## SEV-3 — craft debt

- **DIME-UI-025** | `/feed` date nav + glyph icons | Lens 4 | SEV-3 | VERIFIED | Text characters as icons: `‹ ›` date arrows ([DimeModelFeed.tsx:562,573](../../client/src/pages/DimeModelFeed.tsx#L562)), `▲` [GameCard.tsx:350], `@` as matchup glyph — a fourth glyph source beside lucide (77 icons), 46 inline SVGs, and emoji; lucide set itself carries ~6 duplicated concepts (Check ×5 variants, X ×2, Search ×2, Loader ×2) | One icon family, one optical grid | Standardize on lucide (ChevronLeft/Right etc.); dedupe the icon imports.
- **DIME-UI-026** | dead components carrying banned colors | Lens 9 | SEV-3 | VERIFIED | [components/TeamLogo.tsx:6-11](../../client/src/components/TeamLogo.tsx#L6-L11): 20-color palette incl. retired brand purple `#6c63ff`, `#7209b7`, gold `#ffd166`/`#e9c46a` — **orphaned** (zero importers; every surface re-implements TeamLogo locally); [lib/teamLogoCircle.ts:82](../../client/src/lib/teamLogoCircle.ts#L82) radial-gradient disc — **zero consumers**; [ui/accordion.tsx] never imported; `.feed-tab` CSS block ([index.css:435-481](../../client/src/index.css#L435-L481)) styles a class no component renders — 47 lines of dead `!important` CSS referencing "Barlow Condensed" math | Dead code as provenance tell; banned values one import away from shipping | Delete all four; if a fallback disc is needed, rebuild from law tokens.
- **DIME-UI-027** | unguarded keyframes | Lens 5 | SEV-3 | VERIFIED | `prezGamePulse` ([index.css:496-509]) and BetCalendar's `todayPulse` + local `pulse` ([BetCalendar.tsx:712-716](../../client/src/components/BetCalendar.tsx#L712)) run under reduced motion (not in the kill list); BetCalendar's bare `<style>` redefines the global `pulse` name — if it mounts after the Tailwind layer, all 43 `animate-pulse` sites app-wide silently change animation | Reduced-motion law; keyframe namespacing | Guard both; namespace the local keyframe.
- **DIME-UI-028** | chat suggested prompts · 1280 | Lens 8 | SEV-3 | VERIFIED | Mint prompt pill reads "Best Trends for MLB **July 7, 2026**" on July 31 (`evidence/chat-1280-dark.png`) — hardcoded stale date in a primary empty-state | Copy freshness; transparency promise | Derive the date from the slate; never hardcode dates in prompts.
- **DIME-UI-029** | comment rot teaching banned law | Lens 9 | SEV-3 | VERIFIED | Stale comments describe the retired scheme as current: "JetBrains Mono for all numbers / Barlow Condensed for section headers" [BetTrackerAnalytics.tsx:8-9](../../client/src/components/BetTrackerAnalytics.tsx#L8-L9), [BetCalendar.tsx:8-9]; "LIFETIME gold" [ManageAccount.tsx:7]; layout math "in Barlow Condensed units" [index.css:456,471]; 14 comment-only `#39FF14` references ([MobileGameCard.tsx:439-494], [GameCard.tsx:1250-4944]); `--bt-gold` token name carries mint value ([dime-mobile.css:558]) | Docs-as-law hygiene — comments will re-teach the banned system to the next generator pass | Sweep-delete/rename; the sweep list is enumerated in the color agent's §2-3 (mirrored here).
- **DIME-UI-030** | latent retired font fallbacks | Lens 1 | SEV-3 | VERIFIED | 7 declarations `font-family: var(--dmf-mono, "IBM Plex Mono", monospace)` in [ProjectionCard.css:173,216,232,251,448,509,522] — the fallback is unreachable only while ProjectionCard renders inside DimeModelFeed (which defines `--dmf-mono`); mounted anywhere else it resurrects the retired face; [frozen-tokens.css:304] notes IBM Plex is *loaded but unreferenced* (dead font payload) | Single-font mandate | Replace fallback with the Familjen stack; drop the unused font load.
- **DIME-UI-031** | monoliths | Lens 9 | SEV-3 | VERIFIED | 13 files >1,000 lines: [BetTracker.tsx] 5,293, [GameCard.tsx] 5,086 (9 unextracted sub-components), [PublishProjections.tsx] 2,834, [DimeChatPage.tsx] 2,760; GameCard contains 64 repeated 10-line blocks incl. a 15-prop pass-through triplicated | Component architecture; review-ability | Extract sub-components on next touch; no new code into the two 5k files.
- **DIME-UI-032** | self-verifying comments | Lens 9 | SEV-3 | VERIFIED | `// [VERIFY] <input> → <output> ✓` self-audit annotations at [GameCard.tsx:1278-1280,1695-1697,2821-2823,3564-3565,3931-3933], [MobileGameCard.tsx:966-967,1059]; dated directive citations as authority ("directive 2026-07-29") — the most distinctive AI-authorship signature in the repo | Provenance: a cold reader identifies machine authorship from these alone | Strip verification-narration comments; keep only constraints the code can't express.
- **DIME-UI-033** | dual pseudo-element hit-target workarounds | Lens 2 | SEV-3 | VERIFIED | Same problem solved two ways: `.dmf-sq::after { inset: -9px }` ([DimeModelFeed.tsx:1360-1361]) vs `.pregame-pitchers__lineups::before { inset-block: -6px }` ([ProjectionCard.css:299-314]) — both annotated as audit fixes; visual controls remain 28px/32px | One mechanism per problem | Standardize one hit-area utility.
- **DIME-UI-034** | duplicated wordmark, splits | Lens 6 | SEV-3 | VERIFIED | `/betting-splits` renders the dime wordmark twice on one screen — sidebar brand + centered page header (`evidence/splits-1280-dark.png`) | Wayfinding/brand economy — the mark is chrome, not a page title | Replace the pane-header wordmark with the surface's name (as the feed does).
- **DIME-UI-035** | radii proliferation | Lens 2 | SEV-3 | VERIFIED | App-wide union of rendered corner radii ≥19 distinct values (feed 8: 10/12/8/16/20/26px/50%/composite; landing adds 2/3/4/14/18/22px + three asymmetric composites; tracker 12) vs MASTER.md's 6-value scale | Radius scale discipline | Map all radii onto the `--radius-*` scale; the composites (`4px 12px 12px`) need naming or removal.
- **DIME-UI-036** | console noise in production paths | Lens 9 | SEV-3 | VERIFIED | 189 `console.log/debug/warn` in shipped client components incl. per-render logs in RootRoute ([App.tsx:108-135](../../client/src/App.tsx#L108-L135)); rendered pages log auth state on every mount | Shipping hygiene; the logs narrate internal state to any user who opens devtools | Strip or gate behind DEV.
- **DIME-UI-037** | glassmorphism residue | Lens 3 | SEV-3 | VERIFIED | One rendering `backdropFilter: "blur(4px)"` ([WaitlistAdmin.tsx:99](../../client/src/pages/WaitlistAdmin.tsx#L99), admin-only) + 16 dead `backdrop-blur-*` classes always paired with opaque fills (list in color sweep §6) | Banned pattern (admin scope); dead utilities mislead readers | Remove the inline blur and the 16 inert classes.
- **DIME-UI-038** | Sparkles iconography | Lens 4/9 | SEV-3 | VERIFIED | lucide `Sparkles` rendered in [AIChatBox.tsx:205,250,292](../../client/src/components/AIChatBox.tsx#L205), [MobileChat.tsx:92](../../client/src/features/mobileNav/screens/MobileChat.tsx#L92) (mobile `/m/chat` screen), [ClaudeAssistant.tsx:193] (admin), [SubscriptionPlans.tsx] (admin). The flagship `/chat` surface itself is clean (`evidence/chat-1280-dark.png`). Reachability of `/m/*` from primary nav is limited — `possibly-intentional` scope note | Slop-tell: sparkle = AI badging; brand bans sparkle iconography | Swap for the brand coin-dot or a neutral glyph wherever these screens ship.

## SEV-4 — nits / possibly-intentional

- **DIME-UI-039** | `/bet-tracker` losses tile | Lens 3 | SEV-4 | VERIFIED | "LOSSES 0" renders in `--loss-red` (`evidence/tracker-1280-dark.png`) — inside the scoped bet-loss carve-out ([index.css:161-162]) but red for a zero-loss state is emphasis, not information. `possibly-intentional` | Negative-state law (grey) vs the carve-out | Use red only when losses > 0.
- **DIME-UI-040** | landing `+` crosshair markers | Lens 3 | SEV-4 | VERIFIED | Decorative `+` markers at 2.37:1 ([census-landing-1280-dark.json]); decorative, so exempt from WCAG text rules — filed as intentional-check only. `possibly-intentional` | — | Confirm decorative role (aria-hidden) and leave.
- **DIME-UI-041** | tab-bar CSS `!important` ladder | Lens 9 | SEV-4 | VERIFIED | [index.css:444-481] three breakpoint tiers of `!important` overrides for the dead `.feed-tab` class (see DIME-UI-026) with hand-math comments | Dead weight | Delete with DIME-UI-026.
- **DIME-UI-042** | stale reduced-motion census comment | Lens 9 | SEV-4 | VERIFIED | [index.css:310-313] says "33 animate-pulse usages across 10 files"; actual is 43/12 (motion sweep §3) — the comment's census drifted | Comment accuracy | Update or drop the count.
- **DIME-UI-043** | focus-ring reveal is animated | Lens 5/7 | SEV-4 | VERIFIED | The `.summary__next` box-shadow transition makes the keyboard focus ring **fade in over ~160ms** from transparent (settled probe: resting `none` → focused `rgb(69,224,168) 0 0 0 3px`; instant sampling reads transparent — that timing artifact is what invalidated the original DIME-UI-002) | Apple: feedback on the input path is instant; focus indication should not ease in | Exclude `box-shadow`/outline from the transition on focusable controls, or transition only the resting shadow.
- **DIME-UI-044** | dead variant focus rule | Lens 9 | SEV-4 | VERIFIED | [ProjectionCard.css:591-593](../../client/src/components/projections/ProjectionCard.css#L591-L593) declares a **white** focus ring for the no-edge arrow, but the cascade resolves mint at equal-or-higher precedence — the rule never renders (settled probe shows mint on both variants). Dead code encoding a contradicted intent (mint-everywhere law vs a white variant ring) | D-FOCUS-UNIFY: one mint ring everywhere | Delete the white-ring override (or, if a neutral ring was intended for no-edge, make it win and document the exception).

## Hypothesis verdicts

| H | Verdict | Evidence anchor |
|---|---|---|
| H1 edge badge not one system | **VERIFIED** | DIME-UI-008 |
| H2 three affordances, same destination | **REFUTED** — arrow = carousel advance ([SummaryCarousel.tsx:98]), "View full…" = in-place popover ([ProjectionMarketsPopover.tsx:107-119]), panel icon = decoration inside that same button (aria-hidden). Real finding: the card navigates nowhere at all | — |
| H3 pitcher block above edge, fold risk | **VERIFIED** | DIME-UI-009 |
| H4 two competing "current" signals | **VERIFIED** | DIME-UI-010 |
| H5 bottom chrome collision / 100vh | **REFUTED as stated** — nav is top-fixed ([mobileFloatingNav.css:13-16]) with top safe-area; feed pairs `100vh` with `dvh` ([DimeModelFeed.tsx:1317]). Residual: 5 unguarded `100vh` sites on secondary pages (MlbBacktest 180, WaitlistAdmin 383, ClaudeAssistant 130, CheckoutPage 616, landing-v2.css 44) — folded into craft debt | — |
| H6 light theme = inversion | **REFUTED** — light theme is a designed token system with its own border/shadow/elevation values ([index.css:199-235,274-282]); feed light measured 0 contrast failures (`census-feed-1280-light.json`); `evidence/feed-1280-light.png` | — |
| H7 headshot crop variance | **VERIFIED** — dual-source pipeline with per-source `object-fit`/scale/origin ([ProjectionCard.css:199-213]); MLB = background-removed PNG, Rotowire = raw studio JPEG; mixed rows cannot match head size/eyeline ([MlbPregamePanel.tsx:19-23]) | filed as part of craft debt; SEV-3 by rarity of mixed rows |

## Pulled findings (evidence did not hold)

- **DIME-UI-002 "focus-invisible no-edge arrow"** — pulled during the final pointer re-resolution. [ProjectionCard.css:591-593] defines a `:focus-visible` ring the original agent quote omitted, and a settled measurement (focus + 350ms) computes `rgb(69,224,168) 0px 0px 0px 3px` on both edge and no-edge arrows. The instant-sample transparent reading in the keyboard walk was the box-shadow *transition* mid-flight, not a missing state. Residual truths filed as DIME-UI-043 (animated focus reveal) and DIME-UI-044 (dead white-ring rule).

- "Gradient discs on every team logo" — `teamLogoCircle.ts` has **no consumer**; reclassified as dead code (DIME-UI-026).
- "Numeric column jitter" — Familjen Grotesk's digits measured uniform-width (111 vs 999 identical at 170px/100px-em, `evidence/probes.json` → tabular-digits); feed odds additionally carry `font-variant-numeric: tabular-nums`. No jitter exists. Splits percentages lack the property but inherit uniform digits.
- Landing "black-on-black mint buttons" from the secondary probe — probe walker bug (skipped self background); the census walker shows those buttons pass. Only the white-wordmark case (DIME-UI-001) is real.
