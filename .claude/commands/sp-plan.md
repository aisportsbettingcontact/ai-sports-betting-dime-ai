Use the superpowers:writing-plans skill to turn this into an implementation plan:

$ARGUMENTS

Produce: phased steps with concrete tasks, files touched per task, verification per phase, risks/unknowns, and explicit out-of-scope. Do not start implementing.
