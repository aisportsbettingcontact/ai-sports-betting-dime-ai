Use the superpowers:test-driven-development skill for:

$ARGUMENTS

Red → green → refactor. Write the failing test first, show it fail, implement minimally, show it pass. No implementation before a failing test exists.
