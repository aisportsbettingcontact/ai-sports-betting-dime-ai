Use the stripe-best-practices skill to build/modify/review Stripe work:

$ARGUMENTS

Ground in this repo's existing integration: server/stripeWebhook.ts, checkout/subscription flows behind /pricing and /account, plan labels in client/src/pages/Profile.tsx. Check webhook signature handling, key management, and deprecated-API usage against the skill's references.
