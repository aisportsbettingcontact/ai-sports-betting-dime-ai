Use the superpowers:dispatching-parallel-agents skill for these independent tasks:

$ARGUMENTS

Confirm the tasks share no state or ordering dependency, then dispatch concurrently and synthesize the results.
