Use the design-system skill for token/system work:

$ARGUMENTS

Three-layer architecture (primitive → semantic → component). The Dime values in design-system/dime-ai/MASTER.md are the locked source of truth — extend, never replace.
